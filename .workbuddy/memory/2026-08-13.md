# 2026-08-13 工作日志

## 接入 6789api 中转站生图模型（bananapro + banana2）

- 任务：给 Infinite Canvas 接入两个生图模型 `gemini-3-pro-image-preview`（Nano Banana Pro / bananapro）和 `gemini-3.1-flash-image-preview`（Nano Banana 2 / banana2），走 6789api 中转站。
- 关键结论：这两个 gemini 模型在中转站走的是 **Google 原生 generateContent 格式**（`/v1beta/models/{model}:generateContent`），不是 OpenAI 兼容的 chat/completions。OpenAI 兼容只在 gpt-image-2 这类模型上（`/v1/images/generations`）。
- 修复的核心 Bug：`backend/api/unified_api.py` 的 `_build_gemini_payload` 里参考图字段原本写的是 snake_case `inline_data`/`mime_type`，Google REST 线走 proto3 JSON 映射只认 camelCase `inlineData`/`mimeType`，会报 400。已改。
- 同步加固：`contents[0]` 补了 `role:"user"`。
- 新增供应商：`providers_data.json` 加了 `provider_6789api`（api_url 裸域名 `https://www.6789api.top`，不能带 /v1 否则拼出 /v1/v1beta 错误路径；api_key 留空待用户填）。
- 认证头：`Authorization: Bearer {key}`（sk- 开头，OpenAI 中转站惯例）。
- 待办：① 用户填 6789api 真实 api_key；② 真实跑通出图需运行 pywebview 后端 + 有效 key，尚未实测。
- 遗留：项目里 `image_api.py` 的 outpaint 扩图链路用的是 `x-goog-api-key`，与主链路 Bearer 不一致，后续若要 outpaint 走 6789api 需统一鉴权头。

## 前后端对接核对（2026-08-13 补充）

- 结论：前端 `src/v1/` 生图链路与后端 `main.py` 已完全对齐，无需改。
- 调用链：`run-engine.ts` → `Backend.generateImage` → `unified_generate_image`(main.py:187) → `generate_image_async` 返回 task_id → `poller.ts` 轮询 `unified_get_task_result`(main.py:205)。
- 参数字段一致：model(前端拼 provider_id:model_id，后端按 : 拆分)、aspectRatio、resolution(1k/2k/4k→映射 1K/2K/4K)、referenceImages、count。
- 返回结构一致：poller.ts 读 `r.image_url`(snake_case)，后端 `_parse_gemini_response` 返回 image_url/images，字段吻合。
- 关键资产：`src/cards/ai-draw-api.ts` 的 `_getImageModels` 拼模型 id 用 `${p.id}:${m.id}`，是 model 路由能生效的前提。

## 本地运行环境修复（2026-08-13 补充）

- 依赖：`requirements.txt` 只有 requests/Pillow/pywebview，用户系统 Python 3.12 已装齐，`import webview/requests/PIL` 均 OK。
- 坑1：WorkBuddy 沙箱里 vite build 清空 `gui/dist` 时被 safe-delete 拦截，报 trash 操作失败。解决：手动删 `gui/dist` 目录后重新构建即可（仅沙箱内出现，用户自己跑不会有）。
- 坑2：用户的 Node/npm 是 WorkBuddy 管理的（`.workbuddy\binaries\node\...`），不在系统 PATH。用户自己跑 `python main.py` 报「未找到 Node.js/npm」。已在 `main.py` 的 `build_frontend_for_development()` 加容错：npm 找不到但 `gui/dist/index.html` 已存在时跳过编译、直接复用现有 dist 启动。
- 结论：现在用户直接 `python main.py` 可启动；改前端 TS 后需手动装 Node 或让 AI 代构建。

## 设置面板 4 个 Bug 修复（软件开发团队 SOP）

- 需求：设置面板填不了 url/key/模型；生图模型选择菜单空；设置下拉框是原生 select；删除供应商用浏览器原生 confirm。
- 根因：`src/v1/ui/settings-panel.ts` 是半成品（只有加名称/启停/选默认模型/删除），缺 url/key/模型编辑、测试连接、拉取模型。
- 修复（快速模式：工程师实现 + QA 回归）：
  - `src/v1/api.ts`：Backend 补 fetchModels/testConnection/addChatModel/removeModel 透传。
  - `src/v1/ui/settings-panel.ts`：完整重写（列表卡 + 编辑态含 url/key/代理/模型管理/测试连接/默认绘图模型）。
  - 新增 `src/v1/ui/select.ts`（createSelect 自定义下拉）、`src/v1/ui/confirm.ts`（confirmDialog Promise 弹窗），彻底去掉原生 select/confirm。
  - `src/v1/styles/app.css` 末尾追加样式（全用 CSS 变量）。
  - QA 抓 2 处返工：① `cmd-panel.ts` 模型菜单不刷新（`_getImageModels` 空态占位 `[{id:''}]` 长度=1 导致 `_ensureModel` 的 length===0 判断失效）→ chip-model 点击改异步先重取再弹菜单；② `settings-panel.ts` 拉取模型后未回写 this.providers → 新增 syncProviderModels + _refreshDefaultModelSelect 局部刷新。
- 结果：typecheck 零错误、vite build 通过，QA 最终回归通过。
- 关键约定（复用）：模型 id 前端拼 `${providerId}:${modelId}`；默认模型存 `localStorage['icv_default_model']`；`fetch_models` 只返回不持久化，前端须 `update_provider(id,{models})` 持久化，合并语义=保留手动 chat + 拉取 drawing 替换旧 drawing（参照 `src/services/model-service.ts` fetchDrawingModels）。

## 图片生成节点放开文生图 + 比例补全（2026-08-13）

- 需求：单独新建图片生成节点提示"至少连接一个带图的上游节点"出不了图；比例选项只有 4 个太少。
- 结论1：后端 `unified_api.py` `_build_gemini_payload` 的 referenceImages 本就是可选空数组，纯文生图后端已支持；是前端 `image-gen.ts` 的 canRun 硬性拦了。
- 结论2（查证）：Nano Banana Pro(gemini-3-pro-image-preview) 支持 10 种比例+Auto；Nano Banana 2(gemini-3.1-flash-image-preview) 支持 14 种+Auto（多 1:4/1:8/4:1/8:1 超宽超高）。后端白名单 `gemini_compat.py` `GEMINI_IMAGE_ASPECT_RATIOS` 已是 14 种全集。
- 修复：`image-gen.ts` canRun 删上游拦截（保留 prompt/model 校验）；`cmd-panel.ts` RATIO_OPTIONS 补全 14+Auto（3:4 最前，竖版→方→横版），_renderMultiRefs 无上游 hint 改为"纯文生图可直接生成；连接上游节点可带入参考图"。style-transfer 保持要求上游图（换风格语义）。
- 结果：typecheck/build 通过，QA 回归通过。
- 关键约定：画布运行唯一校验点是 `nodeRegistry.get(type).canRun`（run-engine.ts:32），无额外上游校验。

## 生图失败排查：空 url/key + 后端补校验（2026-08-13）

- 现象：生图失败报 `No connection adapters were found for '/v1beta/...'`（URL 缺域名）。
- 根因：providers_data.json 里供应商 "6789"（provider_7f792137）的 api_url/api_key 都是空串。后端 `_resolve_drawing_model` 在"指定 model"分支不校验 url/key 是否为空，空配置也发请求，拼出相对路径 URL 报技术性错误。
- 修复：`backend/api/unified_api.py` 的 `generate_image` 和 `chat` 两个方法，在拿到 provider 后加空配置校验，缺 url/key 时抛 `AppError(503, "供应商「{name}」尚未填写 API 地址或密钥…")`，语义友好。
- 待办：用户需在设置面板给 6789 供应商填 api_url=`https://www.6789api.top`（裸域名，不带 /v1）+ 真实 key。
- 关键坑：managed python(3.13) 无 requests，测后端要用系统 Python `C:/Users/zeng-rong/AppData/Local/Programs/Python/Python312/python.exe`。

## 三节点合并为统一生成节点（标准 SOP，2026-08-13）

- 需求：用户觉得三节点太相似，拍板合并成单一「生成节点」+ 保留连线流水线。
- 流程：产品经理 PRD → 架构师设计（落盘 docs/system_design.md + 时序图/类图 mermaid）→ 工程师实现 → QA 回归，全通过。
- 改动：删 `src/v1/nodes/product-image.ts`、`style-transfer.ts`；改 14 文件。NodeType 收敛 image-gen、FlowNode 增 refImages、getReferenceImages 唯一入口、canRun=prompt+model、canConnect 防环、persistence migrateNode 迁移（version 3.1）。
- 结果：typecheck 0 错误、build 通过、QA 回归通过。
- QA 观察项（非阻断）：①迁移后旧 product-image 节点 imageUrl=null 需补提示词重跑才喂下游；②addRefImage/removeRefImage 仅标自身 stale、不传播下游（重跑成功后 markUpstreamChanged 补标）。

## 拖图占位全图显示修复（2026-08-13）

- 现象：拖图进空白，卡片主视觉空态 + 图只在底部小缩略图（不是全图）。
- 根因：拖图走 addNode('image-gen',{refImages:[src]})，imageUrl=null；card-view 主视觉只看 imageUrl，参考图在底部 pcard-upstreams 缩略行。
- 修复：`card-view.ts` updateCard 主视觉回退 `mainSrc = imageUrl || refImages[0]`；empty 类/查看大图改 mainSrc；_refStrip 在无输出图时排除占位图 refImages[0] 避免重复。
- 语义约定：卡片主视觉 = 输出图 imageUrl，回退第一张参考图 refImages[0] 作占位；出图后切回输出图。

## 比例映射两处修复（2026-08-13）

- 现象1：选 Auto 比例，参考图 3:4，返回 1:1。
- 根因1：`normalize_gemini_aspect_ratio('Auto')`→None→不传 aspectRatio→Gemini 默认 1:1，无"跟随参考图"逻辑。
- 修复1（后端）：`gemini_compat.py` 加 `nearest_aspect_ratio(w,h)`（宽/高映射到 14 项白名单最近项）；`unified_api.py` 加 `_infer_aspect_from_ref(data_url)`（PIL 解码 data URL 取尺寸），`_build_gemini_payload` 里 Auto 且 ref_images 非空时回退推断第一张参考图比例。
- 现象2：拖竖图 3:4 卡片显示成横版。
- 根因2：`interactions.ts` 拖图 `ratio=naturalHeight/naturalWidth`（高/宽），但 node.ratio 语义是宽/高（defaultRatio 3/4=0.75=3:4 竖版，cardHeight=CARD_W/ratio）。
- 修复2（前端）：改为 `naturalWidth/naturalHeight`。
- 语义约定：Auto = 有参考图则跟随第一张参考图比例，无参考图则模型默认(1:1)；node.ratio = 宽/高。

## 测试方法论观察（用户提问引出）

- 今日多轮修的几乎都是"体验预期错位"型 bug（拖图显示缩略图而非全图、Auto 不跟随参考图、比例反了、下拉/删除确认等），共同特征：代码"自洽正确"（typecheck/build/逻辑走查全过），但实现语义 ≠ 用户（设计师）的第一直觉预期。
- 结论：本项目是"体验敏感"型，自动化检查只保证"不崩"，保证不了"符合直觉"；体验 bug 主要靠用户真实使用暴露。
- 协作方式：关键路径（拖图→写提示词→选模型/比例→出图）每次改完建议让用户做 2 分钟真人冒烟测试；踩过的坑可沉淀成"检查清单"逐条对照。

## 首次推送 GitHub（2026-08-13 晚）

- 背景：项目早已 `git init` + 关联 `github.com/lianggebanjuzi/infinite-canvas`（远程 5 个旧提交），本次把节点收敛 + 供应商面板等 11 个提交一次性推上去（eacc195..e75ddce）。
- 环境坑1（代理）：git 全局代理原来配的是 `127.0.0.1:10793`（已失效），FlClash 实际 HTTP 端口是 **7890**。已更新 `git config --global http.proxy/https.proxy = http://127.0.0.1:7890`。
- 环境坑2（认证）：本机 git 无任何 GitHub 凭据，HTTPS push 报 `could not read Username`。用户生成 classic PAT（repo 权限）后，用 `https://x-access-token:<PAT>@github.com/...` 推送成功；凭据已存 `~/.git-credentials` + `credential.helper=store`，以后本机免密。注意本机 PortableGit 自带了 GCM（`!.../git-credential-manager.exe`），会弹 CredentialHelperSelector，选 manager 即可。
- 环境坑3（引用丢失）：`refs/remotes/origin/main` 文件缺失导致 `git status` 显示 `[gone]`。修复：`mkdir -p .git/refs/remotes/origin && printf '<sha>\n' > .git/refs/remotes/origin/main`，之后 `## main...origin/main` 干净。远程实际一直是最新（ls-remote 可验）。
- 新电脑拉取流程（3 步）：`git clone https://github.com/lianggebanjuzi/infinite-canvas.git` → `pip install -r requirements.txt` → `npm install && npm run build` → `python main.py`。providers_data.json/settings.json 被 gitignore 排除，新电脑需重配供应商。

## 开发日志纳入 git 跨设备同步（2026-08-13 晚）

- 用户担心"换电脑不知道改到哪"，决定把 `.workbuddy/memory/` 开发日志纳入版本管理（含 MEMORY.md + 6 个日志，共 7 个 md，已确认无密钥）。
- `.gitignore` 改为：`.workbuddy/*` 排除全部，`!.workbuddy/memory/` + `!.workbuddy/memory/**` 放行日志目录；其余本地状态（expert-history.json 等）仍排除。
- 提交 `4740d43` 已推 GitHub，以后任何电脑 clone 后 AI 都能看到项目全部历史日志。
- 排障沉淀（PortableGit 环境特有的坑，已全部修复）：
  1. **代理**：git 全局代理 `10793` 失效 → 改 `http://127.0.0.1:7890`（FlClash 实际 HTTP 端口）。
  2. **TLS**：schannel 握手失败 → `git config --global http.sslBackend openssl` 解决。
  3. **凭据**：无 tty 环境 GCM / 内置 helper-selector 都失败（报 could not read Username）→ 清掉全局 GCM 配置，`credential.helper store` + `~/.git-credentials` 明文存凭据。验证用 `printf "url=https://github.com\n" | git credential fill`。
  4. **引用丢失**：`refs/remotes/origin/main` 文件反复缺失导致 status 显示 [gone]，`git rev-parse main > .git/refs/remotes/origin/main` 手动重建。远程实际始终最新（ls-remote 可验）。

## 多结果卡功能：方向B（标准 SOP，2026-08-13 晚，commit 824bf4c + 9b22342，未 push）

- 需求（用户拍板）：生成节点运行不再"结果写回原卡"，改为按 count（1~4）**并发**发 N 个单张生成请求（用户中转站不支持一次多张，确认支持并发），每张完成**立即**自动建一张「结果卡」（新节点类型 image-result）从生成节点连出 → 挑满意的连下游当参考图。
- 关键口径（用户逐条确认）：①count=1 也建 1 张卡（结果永不写回生成节点）；②部分失败：有成功即 done + toast"成功 x/y"，全失败才 fail（旧图保留）；③删生成节点不级联删结果卡（孤儿保留）；④接受旧项目迁移（旧下游重跑后标 stale 需改连结果卡）。
- 架构决策（高见远）：结果卡=新类型 image-result 而非 image-gen 加标记（职责差异清晰，规避"三节点太相似"旧虑）；Promise.allSettled 批次并发 + busy 锁粒度=整批次；parentId 标记 + removeChildren 先清后跑；addEdge 加 suppressStale 参数（防自动建卡连线误标 stale）；dirty.markUpstreamChangedExcept（新卡子树跳过）；persistence version 3.2。
- 实现（寇豆码，13 改 1 新）：新增 nodes/image-result.ts；run-engine runBatch/createResultCard/removeChildren/batchProgress；flow-state getChildren/removeChildren/canConnect 拒 to=结果卡/getReferenceImages 早退/insertStep 守卫；card-view 徽标无入端口；interactions 过滤 creatable、右键无运行项、拖图拒绝；cmd-panel 只读模式+进度"生成中 done/total"。后端零改动。
- 布局修复（QA 发现）：结果卡纵向排布原用"卡自身高度布槽"，混比例时重叠 → 改"累计底部游标"原子占用（9b22342）。
- 验证：tsc 0 错误、vite build 通过、QA 独立回归全 PASS（21 断言冒烟 + 9 项场景推演）。
- 遗留（非本次）：scripts/verify-migration.mjs 的 fontawesome 检查清扫前就失败（src 已无 fontawesome 引用），待裁决是否移除；icv-smoke-test.js 引用已删函数待清理。

## 死代码清扫 P0+P1+P3（2026-08-13 晚，commit 2016bb0，未 push）

- 触发：用户问"全让 AI 写会不会代码越来越重"，顺带要求清扫。清扫分析（工程师只读扫描）：src/ 130 个 TS 中 81 个孤儿（13.7k 行），占 62%；另有 24 个孤儿 CSS。
- P0 删除：`ai-draw-api.ts` 收口（`_getImageModels` 内联进 `src/v1/api.ts` fetchImageModels，删 ai-draw-api/status/card 3 文件）；删 13 处无调用死导出（Backend 6 方法、flow-state.redirectEdge/clear、dirty.clearStale、node-registry.has、link-view.setFlowing、history-drawer.clear、persistence.path）。
- P1 归档：104 文件 `git mv` → `archive/legacy-src/`（保留相对路径可回退；cards 26、components/core/groups/independent/services/state/ui/utils 全量、styles 20、types 4 d.ts 保留 globals、v1 2）。
- P3：删 3 个空 txt（git 未跟踪）。
- 验证：tsc 0 错误、vite build（临时 outDir）通过；主理人抽查关键修复点完好。
- ⚠️ 环境事故：清扫执行中整个 src/ 目录曾从磁盘消失（疑似 sandbox safe-delete 误伤，工作树全 deleted），工程师从 Windows 回收站两个条目无损恢复（$RHS7AFQ / $RDIZWB4.0），逐文件核对一致。主理人二次抽查确认。若再遇此类，先查回收站勿 panic。
- 遗留待办（非本次）：① scripts/verify-migration.mjs 前置检查"main.ts 必须 import fontawesome"清扫前就失败（fontawesome 全项目无引用），是否移除该检查待裁决；② scripts/icv-smoke-test.js / icv-bundle-smoke.js 引用已删函数（nodeRegistry.has、flowState.clear）与旧节点类型（product-image/style-transfer），不在 package.json scripts，建议后续清理。
- 混合提交说明：2016bb0 同时含本会话 4 个 bug 修复（缩放卡顿/比例自适应/Bug3 弹框/Bug4 参考图）与清扫，因 flow-state.ts 等文件同时含修复与删除，按文件粒度拆分风险>收益，接受现状。

## 两个画布 Bug 修复（BugFix 流程：工程师寇豆码 + QA 严过关，2026-08-13 晚）

- Bug1（上传图片后滚轮缩放变卡）：根因 `canvas-view.ts` wheel 处理器每次滚轮同步调 cardView.renderAll()+linkView.renderAll()，而 updateCard 无条件重建 img.innerHTML（大图 background-image + 参考图缩略行，拖入图是 dataURL 大字符串）→ 高频滚轮反复重建 DOM。
- 修复1：wheel 只保留 applyView()（#canvas 整体 transform:scale，卡片/连线 SVG 世界坐标自动跟随缩放，无需重绘）；card-view.ts 加内容指纹缓存（Map 存 mainSrc/refStrip/title/status，逐字段引用比较，内容变才重建 innerHTML；位置/高度/状态 class 每次照常更新）。
- Bug2（新生成图卡片永远竖长方形）：根因 `run-engine.ts` 回写 setNodeImage(nodeId, url, undefined)，ratio 不更新，永远 defaultRatio=3/4。
- 修复2：run-engine.ts 加 loadImageRatio(url)（new Image 取 naturalWidth/naturalHeight，onerror/无效/10s 超时返回 null，Promise 只 settle 一次无泄漏），生成成功后 await 回填真实比例；失败保持原值。
- 验证：tsc --noEmit 0 错误；vite build 需先关闭运行中 app（gui/dist 被 pywebview 占用，safe-delete 报错，环境问题非代码问题，临时 outDir 构建已验通过）。
- QA 建议项（非阻断，后续可做）：① status 参与内容指纹 → 每次状态迁移（idle→run→done/fail）重建一次 innerHTML，可移出指纹（data-status 视觉由 applyCardStatus 每次驱动）；② 生成成功但浏览器加载失败时卡片无错误视觉占位。

## 一键启动器（2026-08-13 晚）

- 痛点：换电脑/换 Python 环境跑 main.py 报 `ModuleNotFoundError: No module named 'PIL'`，用户烦"每次都要手动装依赖"。
- 交付：`launcher.py` + `start.bat`（双击即用）。逻辑：检测 requests/PIL/webview 三个依赖 → 缺失自动 pip install（官方源失败自动切清华/阿里镜像）→ 启动 main.py。已提交推送（2a74253）。
- 本次用户机器是 Python 3.10（`AppData\Local\Programs\Python\Python310`），官方源 SSL 被代理干扰失败，清华镜像装成功（requests/Pillow/pywebview）。
- 备注：启动器用 `sys.executable` 装依赖，哪个 python 调用它就用哪个，不指定版本。
