# 2026-08-28

## 3.0 工作流重构 · 交付批次 A（Phase 0 + Phase 1）完成

- 按《3.0-Infinite Atelier 工作流重构实施规范.md》执行（借鉴 Infinite Atelier 的"图片优先、直接输入即上下文、操作自动产生结果"，不复制其代码/视觉/文案；保留 TS+PyWebView+Python+.icproj 架构与暖白视觉）。
- 团队 software-icv3（主理人+工程师+QA）协作完成，QA 判定 NoOne 可交付。
- **Phase 0 基线**：typecheck 0 错误、build 成功、扩图 smoke 21/21 断言通过。
- **Phase 1 继续创作主路径**：
  - `action-bar.ts`：新增 `canContinueFrom`（唯一守卫）、`createContinueStep` 完善、`nextFreeSlotY`（纵向错开防重叠，有界循环+数学必不重叠）。
  - `interactions.ts`：右键菜单复用共享守卫/函数，与操作条一致。
  - `card-view.ts`：来源摘要（素材=导入素材/派生=基于 xxx，最多四层+visited 防环）。
  - `cmd-panel.ts`：参考图区"来自上游"角标（手动 refImages 无角标）。
  - 新增 `smoke/qa-continue-step.cjs`（32 断言）。
- 零 `.icproj` 格式变化；未新增持久化字段；旧项目加载路径未触碰。
- 用户未提交的 7 个 3.0 小改动保留未覆盖；历史文档删除记录只报告未处理。
- **遗留**：`.icv-qa-current/`（88 文件）与 `smoke/qa-probe-edge.cjs`（QA 临时探针）删除被沙箱拦截+用户拒绝，未删；已把 `.icv-qa-current/` 加入 .gitignore。`cmd-panel.ts` outpaintSettingsSummary 缺 `?.` null 守卫（低危，后续迭代处理）。
- 停止点：等用户试用确认后再继续 Phase 2（统一资源侧栏）。

## 3.0 工作流重构 · 交付批次 B（Phase 2 统一资源侧栏）完成

- QA 判定 NoOne 可交付。typecheck 0 错、build 成功（56 modules）、扩图 21/21、继续创作 37/37、资源插入 40/40。
- **切片 A 统一入口**：双抽屉合并为单外壳 `#left-drawer`，三页签 资源/历史/提示词，单胶囊入口 `#capsule-resources`；页签切换保留查询关键字、关闭保留页签状态；各页签委托原组件渲染（assetDrawer/historyDrawer/promptTab），无复制三份逻辑。
- **切片 B 插入统一**：新增 `src/v1/ui/resource-insert.ts`（insertImageAsAsset / insertHistoryImageToCanvas / attachImageToSelectedGeneration / attachImageToNode / startCreateFromResource），interactions 四处建素材/加参考路径全部收敛。
- 新增：`prompt-library.ts`（提示词共享 store，消除 cmd-panel 两份读写）、`prompt-tab.ts`、`clipboard.ts`、`smoke/qa-resource-insert.cjs`（40 断言）。
- 修复批次 A 低危：cmd-panel outpaintSettings/Ratio/Summary `?.` null 守卫。
- 零 `.icproj` 格式变化；persistence/flow.d.ts/asset-store/history-persist 未触碰。
- **既有失败核实**：qa-text-wiring/node-split/imageperf 等 smoke 的失败均为陈旧测试断言（期望被更早提交有意改变），指向 run-engine/flow-state/api 等本批次未触碰文件，与本批次无关。
- 遗留：`history-drawer.ts` toggle()/close() 死代码（低危，后续清理）；`.icv-qa-current/`（已 gitignore）与 `smoke/qa-probe-edge.cjs` 仍待用户授权清理。
- 停止点：等用户试用确认后再继续 Phase 3（连线可读：参考/文字/结果语义）。

## Git 提交操作

- 用户要求将项目上传到 Git。
- 已提交本地更改：116 个文件变更（6160 行新增，10989 行删除）。
- 主要变更：清理旧文档（2.0 相关文档），新增视频生成、图像编辑引擎、提示词库、资源插入、项目首页等功能，添加 3.0 版本规范文档。
- 提交哈希：9523587。
- 等待用户确认是否 push 到 GitHub（根据跨电脑协作约定，push 必须事先征得用户同意）。

## 4.0 Infinite Atelier 功能复刻 · 全量交付完成（软件团队 SOP）

- 按《4.0-Infinite Atelier 功能复刻总控执行规范.md》执行四卷（4.1 图像编辑/4.2 视频音频/4.3 提示词资产项目/4.4 导演台），团队 software-icv3（架构师审计→工程师分卷实现→QA 全量验收），QA 判定可交付（P0=0/P1=0）。
- **架构师审计核心发现**：3.0 基线已"长出"大量 4.x 零件（裁剪/切图/小地图/提示词库/.icbackup/最近项目/主题/视频链路/蒙版后端），4.0 实质是接管线+补闭环；基线 typecheck 有 4 处死代码错误（image-edit-engine.ts）。
- **新增能力**：蒙版局改（mask-editor）+多角度（angle-editor）+@素材引用（mentions）+ .icbundle 导出导入（bundle_api.py）；视频闭环（startFrame/endFrame/每Key并发1/close-guard）；音频全链路（audio_api.py 三段协议+adapter 表+mutagen 元数据+audio-gen 节点+波形）；媒体资产/历史（MediaAssetRecord+video/audio tab+audio-ref 连线）；模型能力 schema（ModelCapabilitySpec+用户覆盖层+custom-declarative 白名单防注入）；导演台 src/director/* 独立子产品（.icdirector+MP4 Worker 导出+与主画布互通）。
- **关键决策**：音频真实供应商未配置→能力表为空、未配置时不出现可运行按钮（合规降级）；MP4 用 OffscreenCanvas+MediaRecorder Worker；多工程师并行时 main.py 按 audio_/bundle_/capability_/director_ 前缀隔离。
- **验证**：typecheck 0 错、build 双 bundle 成功、py_compile 15 文件全过、director_smoke 8 项 PASS、schema 校验 7/7、icbundle 安全 6/6、P2-1/P2-3 修复复验通过（新增 smoke/qa_p2_1_check_path.py、qa_p2_3_delete_temp.py）。
- 文档：docs/4.0-现状审计与任务分解.md、docs/4.0-QA验收报告-2026-08-28.md。
- 遗留 P2（无害）：build mp4.ts import.meta warning；音频真实供应商待配置。
- ⚠️ 注意：4.0 规范要求卷间停止点（人工验收），本次按用户"一次性推进全部"指令改为卷内自验+末次全量 QA，未做逐卷人工试用验收——启动方式：npm run build 后运行 main.py（导演台：选中图片节点→操作条"导演台"按钮）。

## 4.0 审批退回修复（BugFix 团队 software-bugfix-40-approval）

- 审批意见 docs/4.0-审批意见-2026-08-28.md：暂不通过退回，P1+P2。
- **P1 修复**（backend/director_api.py）：新增 `_validate_director_path`（非空+.icdirector 扩展名）与 `_validate_director_data`（dict+format=='icdirector'），`director_save_project`/`director_save_project_as` 写盘前校验，拒绝时零文件落地（含无 .tmp）；save_as 非法数据弹窗前拒绝。
- **P2 修复**（backend/api/bundle_api.py）：`import_bundle` 新增 `created_assets` 回滚清单（仅实际新建入清单，去重命中不进）+ `project_written` 标志（写盘成功不回滚）；取消/insert 模式非法/异常均清理；新增 `_remove_created_assets` 与 `_atomic_copy` 失败清 .tmp。
- 新 smoke：qa_p1_director_save_validation.py（7 条）、qa_p2_bundle_import_cleanup.py（6 条）；QA 独立复验无绕过/无假断言，既有 smoke 全过（director 8/8、p2-1 5/5、p2-3 6/6、trust-layer 24 项），typecheck 0 错、双 bundle build 成功、py_compile 20 文件全过。
- 报告已更新：docs/4.0-QA验收报告-2026-08-28.md（新增复审轮，P1 遗留改已修复）、docs/4.0-交付总结-2026-08-28.md（复审修复内容小节）。
- 观察项（非本次范围）：bundle_api._rewrite_paths 对缺 sourcePath 的 asset 会把空串也重写，后续迭代处理。
- 未 commit/push（用户未授权）。
