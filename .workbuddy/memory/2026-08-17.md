# 2026-08-17 开发日志（2.0）

## 开工：拉取远程 2.0 + 本机新建 2.0 目录

- 开工先 pull：远程 main 领先本地（本机上次停在 8a321c7，远程已到 42502a0，含 2.0 起步清理 caf6122/7358338、信任层 0bc385c、资产评估层 5be2254、资产库拆分+UI 收口 42502a0 等多轮 2.0 增量）。
- 第一次 pull 遇到 merge 冲突（`2026-08-14.md` 两侧都写了日志），先按 merge 处理到一半，用户纠正：**2.0 是唯一权威、以后只做 2.0，不合并** → 放弃本地独有提交（8a321c7 旧日志补记），`git reset --hard 42502a0` 对齐远程。中途踩坑：`.git/index.lock` 残留（merge --abort 被沙箱拦截），删锁 + 非沙箱 reset 成功。
- **本机目录决策（用户拍板：新建 2.0 文件夹）**：2.0 起步说明里的 `G:\Infinite Canvas\Infinite Canvas 2.0` 是另一台电脑的路径，本机 D 盘只有 `Infinite Canvas 1.0`。用户选「新建 2.0 文件夹」→ robocopy 复制 `D:\Infinite Canvas\Infinite Canvas 1.0` → `D:\Infinite Canvas\Infinite Canvas 2.0`（排除 `deepseek harness/` 1.5G 遗留 + `Infinite Canvas 原始备份.rar` 54M，node_modules/.git 全带）。
- 验证：2.0 目录 git 历史完整（HEAD=42502a0）、工作区干净；**npm run build 通过**（先删 gui/dist 再 build，tsc 0 错 + vite 44 模块）。
- ⚠️ 后续开发请用 `D:\Infinite Canvas\Infinite Canvas 2.0` 目录（当前工作区 IDE 仍是 1.0，需重新打开）。1.0 目录保留作归档，不再开发。

## G 盘工作区同步 + FluxPort 图片链路验证（晚上，本机 G:\Infinite Canvas\Infinite Canvas 2.0）

- 开场 pull：代理 7890 不通（用户代理软件未开），**绕过代理直连拉取成功**（git -c http.proxy= -c https.proxy= pull）；拉回 D 盘电脑当天推的 FluxPort 适配（2eb2804 + 972b915）。⚠️ origin/main 引用陈旧（packed-refs 未随 fetch 更新，status 误报 ahead 8），手动写 loose ref 对齐解决——**这是 G 盘工作区的环境特性，后续 pull 后如遇 ahead 误报可同样处理**。
- **FluxPort API 验证（团队 QA 严过关）**：9 mock 用例 7 过 2 挂，2 挂均为 QA mock 与 972b915 改动不一致（t04 MockResp 缺 url 属性致 origin 空；t05 断言与 mock 自相矛盾），非源码 bug；修正 mock 后 9/9。**真机出图成功**：ai-media.vip 直连 + gemini-3-pro-image-preview，202 异步 → 26.5s 出图 → signed_url 转 base64（1408×768 PNG）。结论：FluxPort 适配已修好。
- **gpt-image 接口链路补全（团队工程师寇豆码 + QA 严过关，提交 19d3341）**：unified_api.py `_build_openai_image_payload` 补齐 resolution(1k/2k/4k)+aspectRatio→size 全档位映射 + 参考图 data URL→payload.image（FluxPort 协议字段待真机确认）；新增 .qa-verify/qa-verify-openai-image.py（10 用例）；修 v2 脚本 D 盘硬编码路径→自动推导。QA 独立回归 10/10 + 9/9，前端闭环确认（gpt-image 可进绘图模型列表、扩图仍强制 gemini 系）。**用户实测：换 gpt 分组 key 出图成功**。
- **多 key 机制确认（用户问"分组后能否拉取别的模型"）**：providers_data.json 本为数组结构，前端设置面板已有完整多供应商 CRUD（添加→填 url/key→测试→拉模型→删除），`fetch_models` 按 key 动态拉 `/v1/models`，节点模型列表汇总所有启用供应商——**"多个 key"功能已存在，无需开发**。实测两 key 各拉各分组：visionary.beer→gpt-image-2/nano-banana 系；ai-media.vip→gpt-image-1.5/2（该 key 分组拉不到 gemini，但 gemini 真机可出图——"拉取列表≠实际可用"，模型组限制按协议格式而非列表）。⚠️ 手动补录绘图模型入口可能缺失（add_chat_model 只覆盖对话模型），待确认是否需补。
- ⚠️ 未处理：qa-verify-fluxport-202.py t17 用例缺陷（mock 未设假时钟，真等 5 分钟超时被杀，与本次无关）；改动已 commit 19d3341，**push 待用户同意**。
