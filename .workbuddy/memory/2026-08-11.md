# 2026-08-11

## 项目评审：Infinite Canvas 1.0（用户主动要求评价）

用户请我评价这个停摆的个人项目。做了全量摸底（前端 src/ 110+ TS 文件、后端 11 py、开发准则 6 文档、构建产物、运行时数据），关键结论：

- **状态确认**：TS 迁移实际已 100% 完成（非文档记录的"规划中"），main.py 已切 gui/dist；瘦身重构开了头（card-shell.ts、drawing-board/features 已拆出）；最后一次活动 2026-07-03/04（TS 测试版 exe + 最终构建）；零测试文件。
- **真实使用证据**：providers_data.json 有 bltcy.ai "柏拉图" 供应商真实 key；prompts_library 有"生成复现图 json + 参考法线图/深度图"提示词——用户真用它跑自己的电商三维视觉工作流。
- **核心评价**：工程超配（文档纪律、错误分层、中转站格式兼容、渐进迁移）、产品缺位（通用 AI 画布赛道正面撞 ComfyUI/白板/各 AI 工具，无"非它不可"差异点）；死因是只升级工程不升级产品 + 零测试导致不敢改 + dogfooding 只用了 20% 能力。
- **给出方向**（口头，未落文档，尊重用户偏好）：① 垂直化收窄为电商/三维视觉 AI 工作流工具，用自己行业 dogfooding；② 或开源换社区反馈；两条路都先补测试。
- 用户后续可能想深入掰扯"复活方案"，待命。
- 【追加】用户两轮补充真实停摆原因：① 不知道做这个有什么意义（没形成依赖）；② 当时 AI 生图 API 太贵 + 功能做得不好。三者实为一条负反馈循环（贵→用得少→功能糙→没意义→停摆）。注意：用户至今仍用 bltcy.ai 代理生图（nano-banana-pro 技能），说明 AI 生图需求没消失，只是绕过了这个重工具——需求在，载体换了。
- 【追加】用户决定重启，但纠结差异化："有意义？是不是比开了会员的网站好？可能就只是便宜？"——我的判断已给：便宜是死路（拼不过价格战），真正的空间在"生图之后的工作流编排"（会员站流程割裂、数据封闭、不可编程），且生图价格已崩，"生图能力"不再稀缺，围绕生图的工作流才稀缺。重启验证标准 = 用户自己每周愿意打开的频率（dogfooding）。
- 【追加·关键产品洞察】用户亲口对比：LiblibAI 工作流比他原来的 Infinite Canvas 卡片好用——"前端换一个关键词，后面都可以一次性改"（参数化联动重跑）。他判断"卡片不对"= 产品模型错了：Infinite Canvas 是自由画布编辑器（搭建成本高），Liblib 是参数化模板执行器（改参数→整链重跑）。重启方向已明确：砍自由性，做"一条流水线 + 前端参数面板"，复用现有 PipelineEngine/供应商层/本地存储资产。用户场景（3D 复现、园艺电商视觉）Liblib 通用模板覆盖不了，是差异化空间。
- 【追加·UI 方向已定】用户 UI 拍板：① 玻璃极简风推翻，要重做设计；② 布局沿用现有骨架；③ 核心操作（打开/保存/全部运行/主题/设置）放底部居中浮动工具栏；④ 运行状态用"流光连线"表达，不暴露日志/状态文字；⑤ 支持框选节点→"运行选中"只跑选中的段；⑥ 节点显示图+关键词+模型名+比例+分辨率；⑦ 砍掉自动切供应商容错（用户说没必要）；⑧ 允许以后自建节点/连线（注册式架构）。已交付独立 HTML 原型 prototypes/ui-v2.html（深色精致技术风，可交互演示运行流光/框选/状态角标），未改动现有代码，等用户审。
- 【追加·原型迭代 3 版定稿方向】v1 深色技术风被否（"不好看，要温馨"）→ v2 温馨园艺风（暖米白+鼠尾草绿+大圆角）→ 用户发即梦智能画布截图要求"图即卡片"形态 → v3：大图卡片+悬浮标签/按钮+上下文悬浮条（贴卡片上下沿、跟位）+底部指令面板（参考/标记/风格+输入框+参数chip+发送钮）。用户确认悬浮框要选中卡片才出现、且贴在卡片上下。配色保留温馨系。
- 【追加·团队 spawn 失败 + 总结文档】用户质疑"怎么一直是你在干活，团队呢"——承认流程失误（总监代写了设计的活）。TeamCreate "infinite-canvas-reboot" 成功，但 spawn 设计师两次 prompt 被截断失败，用户放弃（"算了"）。按用户要求输出《重启方案总结-2026-08-11.md》于项目根目录，涵盖评审结论/死因诊断/重启依据/产品定位/UI 决策/架构决策/下一步。团队尚未正式运转，下次开工需重新 spawn 成员。
- 【追加·专家团正式启动（第二次）】用户选择 MVP开发专家团 并出示重启方案总结。Phase 0 完成，四项关键答复：① 原型"还要改"（具体修改点待用户补充，由总监转交颜好看）；② 前端起点=架构师评估后定；③ 失败处理第一版只做红点+原因（不做一键换源）；④ 真实失败主因=网络问题（超时/代理不稳，非审核拦截）→ 网络层健壮性仍是重点。已修正 MEMORY.md 过时方向（自动容错从"灵魂"改为"已砍"，UI 从玻璃极简改为温馨园艺风）。TeamCreate "infinite-canvas-v2" 成功，许清楚（PRD 蒸馏）、高见远（代码盘点+改造方案）、颜好看（原型精修+Token）三人并行 spawn 成功、后台运行中。注意：这是用户第二次质疑"团队在哪"，本次必须让团队真实产出、结果经总监中转汇报。
- 【追加·本会话 Phase 0 二次澄清 + 关键层级纠正】用户两处纠正（重要，防再犯）：① 第一版本质是**工作流引擎**（注册式节点+连线+参数联动+分段执行），"输入产品图→换风格"只是验证机制的 2 个示例节点，不是"换风格工具"；② **工具与供应商解耦**——供应商是用户配置（providers_data.json），工具不做"默认哪家"决策，问"用哪家"是问错了层级。验收样本开发完再试；启动方式=先过 UI 原型终审。
- 【追加·原型终审 + 图库改悬浮抽屉】对 ui-v2.html 做代码级核对：方案 9 项 UI 决策全部落地（Token/双主题/布局/图即卡片/选中上下沿悬浮/三标签指令面板/五态状态点/扫描光+流光连线/红线合规）。用户终审提出唯一修改：**历史图库从固定侧栏改为左侧悬浮抽屉**——默认收起不占画布，左边缘中部竖条把手（SVG 箭头，展开时旋转 180°），点击展开/收回，顶栏图库按钮同步联动；面板悬浮于画布上方（z-index 22，低于操作条 25/指令面板 30），圆角+暖阴影。已改完三处（CSS/HTML/JS），等用户预览确认。待确认点：风格标签页参数面板形态、图库拖入手势体感、深色暖棕黑冷暖度。
- 【追加·原型 UI 评审与两条新决策】我对 ui-v2.html 做了一轮设计评审：色彩只做了单色系绿（缺陶土橘/蜂蜜黄暖点缀）、三级文字偏脏、圆角 7 档混用、连线中点+号常驻、端口热区太小、空态为零等。两条已拍板修正：① **卡片尺寸随图片比例自适应**（原 280×200 横版定死被否，电商图多为竖/方图）；② **指令面板维持"贴卡片下沿"、不吸底**——用户理由：吸底后不知道当前作用的是哪张卡。因此需要补的解法是：贴卡 + 智能避让（下方空间不足翻转/钳制）+ 面板内显式标识当前卡片（缩略图+名称）+ 多选锚定不跳。尚未动手改原型，等用户确认是否出 v4。
- 【追加·v4 原型已出（覆盖 ui-v2.html）】用户拍板第三条决策：**多选时操作条和指令面板都不出现，仅单选显示**（解决多选锚点跳动与"作用于谁"歧义；多选的"运行选中"入口留待后续右键/快捷键）。v4 落地项：卡片宽度固定 260、高度随 ratio 自适应（含 1:1/3:4 混合演示）；流水线卡片水平对齐；面板智能避让（下方不足翻到卡片上方+操作条互换+横向钳制）；面板顶部"正在编辑·卡片名"标识行（缩略图+状态提示）；暖棕次级点缀 --warm（发送钮 hover/NEW 角标/未保存圆点）；圆角收敛 8/12/16 三档；标签改暖白底药丸；连线中点+号 hover 连线才出现；端口 ::after 扩热区至 25px；新增 c4 虚线空步骤卡（空态形态演示）；顶栏未保存暖点（改名/发送触发、保存清除）；扫描光加深。等用户预览。
- 【追加·配色去浊】用户反馈整体"有点脏、不舒服，是不是太黄"。诊断：主因不是黄而是"浊"——所有面明度挤在 92-100% 且全带黄橙调、阴影棕色、点阵比底色更黄。改法：基底改净（bg-app #FAF9F6 微暖近中性、面板纯白 #FFFFFF、阴影改中性灰 rgba(60,54,46)、点阵 #E8E5DF 变淡、边框去黄），暖色只留 accent/warm 点缀。深色主题同步去棕（#232220 系）。关键原则记录：**温馨感靠点缀色+圆角承担，基底必须干净**——这对后续所有界面生效。等用户对比确认。
- 【追加·去浊第二刀】用户反馈"还是脏"。重新诊断出真正主因：**占位图的 CSS 渐变本身是浑浊橄榄绿/卡其色调**，三大块浊色主导画面，chrome 再干净也没用；帮凶是细边框过多+点阵偏密。已改：三张占位渐变整体提亮通透（高明度奶绿/米白系）、浮层边框 border-strong→border、顶栏/缩略图用新增 --border-soft、cmd-chip 去边框改底色块、点阵 1px/28px 更淡更疏。教训记录：评审原型时**占位素材的色调就是画面的一部分**，不能用"反正后面换真图"忽略。等用户再次确认；若仍嫌脏，下一杠杆是 accent 饱和度与真实图片。
- 【追加·组拖已实现】用户问"全选后移动没效果，是留到后面做吗"——答：不是预留，是原型漏写。已实现组拖：按住已选中的卡片可整组移动（记录每张卡偏移，缩放适配）；配套修正选中逻辑——点击已选中卡片（无 Shift）保留整组选中以便拖动，点空白处取消；多选仍不弹操作条/面板（用户拍板）。等预览确认。
- 【追加·顶部减负 + 底部胶囊条】用户要求：打开/保存/暗色模式/设置从顶栏移到底部居中胶囊条（圆角全圆、含分隔线）；图库图标从顶栏删除（左侧抽屉把手已承担）；顶栏只留 logo + 项目名 + 未保存点。已改 CSS/HTML/JS（toast 上移避让胶囊条），等预览确认。顶栏布局决策：**高频文件操作全部下沉，顶部只留身份信息**。
