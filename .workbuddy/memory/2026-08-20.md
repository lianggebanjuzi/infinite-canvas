# 2026-08-20 开发日志

## AI 创作画布重构（按《AI创作画布_产品与交互重构说明.md》）——软件开发团队 SOP 全流程交付

用户给出重构说明书，要求诊断现状 + 想办法。以「软件开发团队」专家身份走完整 SOP：产品经理（增量 PRD）→ 架构师（增量架构设计）→ 工程师（T01-T05 两批次实现）→ QA（全量回归），**QA 314/314 全绿，路由 NoOne（源码零 Bug）**。

### 关键文档（已入库 docs/）
- `docs/重构-增量PRD-2026-08-20.md`（许清楚）
- `docs/重构-增量架构设计-2026-08-20.md` + `docs/sequence-diagram.mermaid` + `docs/class-diagram.mermaid`（高见远）
- `回归报告-批次队列重构-QA验证-2026-08-20.md`（严过关）

### 用户拍板（4 项）
1. 实施范围：全部 T01-T05（A 收尾 + B 批次队列 + C 界面分层）
2. **卡片最大高度保持现状 520**（不采纳 PRD 的 300/180/260）
3. count>1 **废除自动建子卡**，统一节点内 generatedImages 浏览（历史子节点不删兼容）
4. cmd-panel 编辑职责**全部迁入右侧属性编辑器**（代码保留、编辑职责下线）

### 交付内容（9 个本地提交：174be31..0e43073，未 push）
- **T01 数据层**：NodeStatus 五态→七态（idle/queued/run/done/partial-failed/fail/stale）；GenerationBatch/GenerationJob 类型；新增 `nodes/port-types.ts`（五类端口契约表）、`state/batch-store.ts`（会话级 store）、`engine/batch-queue.ts`（限流队列，并发 localStorage['icv_batch_concurrency'] 默认 2 clamp 1~3）
- **T02 A 收尾**：输入保护审计全过；卡片 520 封顶（text-split 卡原无上限已修）；canConnect 改「基础校验→特例→查表→防环」不退化
- **T03 引擎改造（核心）**：runBatch 删 Promise.allSettled → batchQueue 限流执行；Job 独立 error；retryJob/retryFailed；count>1 与拆分统一写回 generatedImages（createResultCard 保留供锁定/扩图/兼容）；trace/history 补 batchId+jobId；刷新 rebuildFromNodes 重建已知批次（.icproj 仍 3.4）
- **T04 底部任务面板**：`ui/task-panel.ts` #1~#N 网格（缩略图/失败原因/单条+全部重试）+ 汇总「成功 x/y 失败 z」+ unknownCount；bottom-bar 加「任务」按钮
- **T05 界面分层**：`ui/property-editor.ts`（右侧栏编辑：长文本/拆分槽位/模型参数/参考图）、`ui/result-viewer.ts`（右滑抽屉：批次大图/下载/复制/反推/再编辑/重试）、card-view 卡内 prev/next + 七态徽标、连线端口高亮 + 语义 toast、cmd-panel 编辑职责下线
- **竞态修复**：batchId 跨毫秒 bug——createBatch 增加可选 id，runBatch 透传预计算批次号（QA 打桩 6 连跑验证）

### 测试
- 新增可复用回归：`smoke/qa-batch-queue.cjs`（86）、`smoke/qa-behavior-accept.cjs`（58，§11 行为验收）、`smoke/qa-race-batchid.cjs`（30）
- `qa-text-wiring.cjs` 更新到 batch-store 新 API（140/140）；旧断言 6 条确认为刻意行为变更（指令保留/count 废子卡/prompt 预览禁用）
- 存量旧测试 qa-dual-cards.cjs、test-textgen.cjs 断言基于旧反推走线行为，建议后续清理（P2-4）

### 待办/下一步
- push 未做（用户未同意，本地 9 提交在案）
- 旧测试清理（qa-dual-cards/test-textgen）建议后续处理
- result-viewer 对「全失败且节点无图」批次提示无图可看（任务面板兜底）；属性编辑器窄窗口占 276px
- 阶段 D 展望（端口注册表收口特判、多选复制粘贴、自动化测试完善）未实施

## ⚠️ 回退事件（同日 13:34，用户拍板）

**用户看后反馈"回退吧，不是我想要的"**——整个批次队列重构（9 提交 174be31..0e43073）已全部回退，HEAD 回到 `296d9ec`（重构前）。

- **备份**：tag `backup-refactor-20260820` 指向 0e43073，9 个重构提交完整可恢复；回归报告归档在 `backup-refactor-2026-08-20/` 目录
- **过程坑**：`git branch backup/...` 分支名含斜杠疑似被 Git Bash 路径转换干扰（未生效），改用 tag 成功；第一次 `git reset --hard` 后工作区大量文件缺失（status 显示 D），**第二次 reset 才完整恢复**——遇到 reset 后文件缺失，重跑一次 reset 或 `git checkout-index -a -f` 修复
- 遗留未跟踪项：`.workbuddy/memory/`、`.zcode/`（IDE 产物）、`backup-refactor-2026-08-20/`（归档）
- 教训：本次方案（批次模型 + 四层分层 + 废除自动子卡）技术验收全过，但**用户对交互方向不满意**——先弄清用户真实想要的交互再大动干戈；重构前应先用原型/小步验证方向，而非一次做完再回退

## 批次图叠放 deck（2026-08-20 晚）
- 需求：文本拆分→生图节点多图返回时卡上只能看到一张，要微信式叠放效果。实现：`card-view.ts` 骨架加 `.pcard-stack` 容器（DOM 在 .pcard-img 前，靠绘制顺序压在主图下，transform 层不影响），`_deckLayersHtml` 循环取模取当前封面后 1~2 张作露边层；「展开 N」胶囊按钮（`.pcard-expand`）右下角，点击走 `_bindGalleryEvents` 委托直达 resultViewer.open；`.has-deck` 时尺寸标注 bottom 30px 让位，fail 态按钮隐藏。数据流零改动。本地提交 1744e51（未 push，用户规则：push 需先征得同意）。构建前已手动删 gui/dist。
- 坑：用用户参考截图当测试图会把截图里自带的 UI 当成 bug 表象——验证视觉效果要用干净素材。

## 叠放演示项目 + persistence 修复（2026-08-20 深夜）
- 用户想直接在画布看叠放效果 → 生成 `叠放效果演示.icproj`（项目根目录，临时）：text-split + image-gen 两节点一线，image-gen 带 4 张 canvas 生成的 dataURL 批次图，应用内「打开项目」即可看 deck + 展开。
- 顺带发现并修复两个静默丢数据 bug（本地提交 2f84f33，未 push）：①migrateNode 丢弃 generatedImages/activeGeneratedIndex/imageWidth/imageHeight——重开项目批量画廊即失效；②text-split 类型未收录进 migrateNode——重开项目时拆分节点整卡连线一起消失。已加归一透传 + text-split 分支。
- 已知未动：migrateNode 把 node.trace 置 null（生成档案重开丢失）——牵扯复现功能语义，未擅自改，下次和用户确认。

## 移除复现按钮 + 扇形排列展开（2026-08-20 深夜，用户两次拍板）
- 复现按钮确认为伪需求（配方信息保留即够）：删操作条/右键菜单/资产库三处入口 + reproduce.ts + bridge 挂载（本地提交 24f29fa）；历史图库本无按钮只改了注释。trace 照常生成供信息栏。复现功能相关讨论：用户曾以为自己早删了，实际 2.1 一直有四个入口。
- 「展开 N」改为卡片右侧扇形排列（提交 af9f118）：_expandedFans 会话态 Set 存展开节点；扇形排渲染在 .pcard-stack 容器（pointer-events:auto 恢复）；_fanStripHtml 错峰 animation-delay 45ms 入场；点缩略图=setCover+收起；.has-fan z-index:15 浮于邻近卡。deck hover 微展开用 .pcard:hover .stack-layer 纯 CSS。顺带修复：批次控件 mousedown 之前会误触发卡片拖拽（interactions.ts guard）。
- 已知边界：扇形排横向最长 = 图数×128px，图很多时会伸出屏幕——后续可考虑换行或滚动。

## 推送 GitHub（2026-08-20 23:20，用户明确同意"传上去"）
- 推送 e0a71c6..01a5451 共 5 个提交：叠放 deck(1744e51)、persistence 修复(2f84f33)、移除复现(24f29fa)、扇形展开(af9f118)、开发日志(01a5451)。
- 未推送的工作区改动（用户自己的，保持未提交）：backend/api/provider_api.py、unified_api.py、video_api.py、src/v1/api.ts、types/backend.d.ts、ui/settings-panel.ts；未跟踪：assets.json、deck/fan 预览图。叠放效果演示.icproj 被 gitignore 忽略未入库。
