# 问题报告 · 2026-08-15

> 用途：盘点当前项目的现状、遗留问题与待办，供后续（"后面再做"）对照处理。
> 说明：本报告只做记录，不自动改动任何代码 / 不自动 git 操作。

---

## 一、当前状态总览

今天做了三件事，其中两件已闭环，一件（文本节点重构）代码已完成但**尚未验证收尾**：

| # | 事项 | 状态 |
|---|------|------|
| 1 | 无边框窗口顶栏拖动（改用 pywebview 官方 drag-region 机制） | ✅ 完成并提交 `cf98ef4`，待用户实测 |
| 2 | 画布无限点阵 + text-gen 新建不预填/卡片显示关键词 | ✅ 完成并提交 `ff042d4`，待用户实测 |
| 3 | 文本节点重构（文本结果节点 + 图片节点文本反推） | ⚠️ 代码改完、QA 核心验证通过、返工 3 处已改，但**前端未重新构建** |

---

## 二、遗留问题 / 待办（按优先级）

### P0 · 阻断启动，必须先处理

1. **前端未构建**：`gui/dist/` 目录已删除（构建时规避 safe-delete 坑），尚未重建。
   - 后果：`main.py` 启动时检查 `gui/dist/index.html`，不存在会直接报「前端入口不存在」。
   - 处理：在项目根执行 `npm run build`（npm 若不在 PATH，用 `C:/Program Files/nodejs/npm.cmd run build`）。若报 safe-delete/trash 错误，先删 `gui/dist` 再单独跑 build。

### P1 · 功能验证

2. **文本节点重构未实测**（用户未跑过）。需实测以下链路：
   - 新建「文本」节点 → 卡片空态「点击输入文本」→ 点卡片直接改文本（只改结果）
   - 选中文本节点 → 下方命令框选文本模型 + 输入「改得更专业」→ 发送 → 文本模型处理 → 结果回写卡片
   - 图片节点 → 模型切「文本模型」→ 输入「反推这个图片」→ 发送 → 反推文本落到一个新建文本节点
   - 文本改完 → 下游图片节点标「待重跑」（联动）
   - 确认文本节点**没有**顶部操作条、**没有**参考图/入端口连线、**没有**润色按钮

3. **smoke 测试脚本已更新但未跑通验证**：
   - `smoke/qa-textgen-panel.cjs`、`test-textgen.cjs`、`test-textgen-qa.cjs`、`qa-textgen-linkage.cjs` 已按新行为改写，但改写后尚未执行确认通过。
   - 处理：`node smoke/<脚本>.cjs` 逐个跑，失败则修断言（脚本本身应匹配新语义，不是实现问题）。

### P2 · 收尾与清理

4. **git 未提交**：本轮文本节点重构的全部改动都在工作区，未 commit、未 push。
   - ⚠️ 用户明确要求：**push 必须事先征得同意**，本地 commit 也需主动告知。当前未做任何 git 操作。
   - 之前 `cf98ef4`、`ff042d4` 两个提交因本机代理（7890）不通，push 失败，**尚未推到 GitHub**。

5. **可疑临时文件**（工作区里多出的，待确认是否垃圾）：
   - `_b.txt`、`final2.txt`（untracked）。若确认无用可自行删除。

---

## 三、本轮未提交的改动清单（git status）

已改（M）：
- `src/v1/nodes/text-gen.ts`、`src/v1/nodes/image-gen.ts`、`src/v1/engine/run-engine.ts`
- `src/v1/ui/cmd-panel.ts`、`src/v1/canvas/card-view.ts`、`src/v1/canvas/interactions.ts`
- `src/v1/state/flow-state.ts`、`src/v1/ui/action-bar.ts`
- `src/v1/styles/app.css`、`src/v1/types/flow.d.ts`
- `src/index.html`
- `smoke/test-textgen.cjs`、`smoke/test-textgen-qa.cjs`
- `.workbuddy/memory/2026-08-15.md`、`.workbuddy/memory/MEMORY.md`

新增（??）：
- `smoke/qa-textgen-linkage.cjs`、`smoke/qa-textgen-panel.cjs`

---

## 四、文本节点重构的核心变更（供对照理解）

1. **文本节点（text-gen）→「文本结果节点」**：
   - 卡片只显示 `outputText`（结果），点文本直接改（只写 outputText）
   - 下方命令框 = 命令（选文本模型 + 输入指令 → 发送 → 文本模型处理 → 结果回写）
   - 命令框发送后清空、不回填；命令取值加了兜底：`input.value || params.instruction`（修 P1 命令被吞）
   - 撤销：润色按钮、参考图/入端口连线、顶部操作条
2. **图片节点（image-gen）新增「文本模型反推」**：
   - 模型 chip 支持「绘图 / 文本」类型切换
   - 选文本模型 + 命令 → 反推该图 → 结果新建一个文本节点（右侧偏移）
   - 反推结果也记入文本历史（`pushTextHistory`）
3. **联动保留**：文本变化 → 覆盖直接下游 image-gen 的 prompt + 标 stale

---

## 五、建议后续处理顺序

1. 先 `npm run build` 重建前端（否则启动不了）
2. 用户实测第 3 号功能（文本节点 + 图片反推）
3. 跑 smoke 脚本验证
4. 确认无问题后，再决定是否 commit / push（push 需用户同意）
