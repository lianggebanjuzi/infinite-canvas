# UI 空间层级优化 · 2026-08-18

## 本轮原则

本轮只改界面样式，不改功能逻辑、数据模型、事件绑定或生成流程。

## 已完成

- 左侧入口胶囊从画布垂直中心移到左上导航区，减少对画布中心的干扰。
- 历史图库/资产库抽屉由全高侧栏改为浮动工作区：宽度 `280px`，顶部避开顶栏，底部避开运行条。
- 同步调整抽屉把手位置与隐藏位移，避免展开后错位或露出空白区域。
- 历史图库保持两列缩略图，增加面板内边距与间距，提升缩略图可读性。
- 增加 `760px` 以下和低高度窗口的适配规则，保留原有内部滚动与操作。

## 验证结果

- TypeScript 类型检查通过。
- Vite 构建通过；首次构建命中项目已知的 `gui/dist` safe-delete 拦截，清理项目内旧构建产物后重建成功。
- 浏览器验证 `1280×760` 与 `720×520`：抽屉边界、底部避让、历史图库滚动区域正常。
- 验证历史图库/资产库互斥切换及收起正常。
- 页面错误检查为空。

## 未改动

- 历史图库、资产库、拖拽、复现、采纳、锁定、运行等功能行为。
- TypeScript 文件与 HTML 结构。
