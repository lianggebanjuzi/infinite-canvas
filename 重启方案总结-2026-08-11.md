# Infinite Canvas 2.0 重启方案总结

> 日期：2026-08-11
> 性质：项目重启决策文档（一次完整评审 + 产品重定位的结论）
> 状态：方向已定，UI 原型已出第三版，待开发启动

---

## 一、1.0 版评审结论

**一句话：工程上超配、产品上缺位。**

做得好的（都是硬资产，重启直接复用）：
- 工程纪律职业级：开发准则文档体系完整（UI 设计准则、卡片开发规范、迁移方案、瘦身重构计划）
- 后端 `UnifiedAPIRouter` 对国内中转站生态的兼容经验（多格式响应解析、错误分层、代理开关），稀缺资产
- TS 迁移实际已 100% 完成（src/ 110+ 文件，main.py 已切 gui/dist），卡片瘦身重构已起步
- 有真实使用证据：bltcy.ai 供应商配置、提示词库里的"生成复现 json + 参考法线图/深度图"工作流

死因诊断（三个原因实为一条负反馈循环）：
```
API 贵 → 舍不得跑 → 用得少 → 功能没被反复打磨 → 糙 → 更不想用 → 感觉没意义 → 停摆
```
- 产品定位撞车：通用 AI 画布正面刚 ComfyUI/白板/各 AI 工具，无"非它不可"差异点
- 最后三个月只升级内部工程（迁移、瘦身），没升级产品力
- 零测试，改到后面不敢改
- 只用了工具 20% 能力，却为 100% 通用性付了全款维护成本
- 最终一击：离职 → 使用场景消失 → 彻底冻结

## 二、重启依据（为什么现在值得做）

1. **场景回归**：新工作仍是园艺产品电商视觉，每天都在这条链上
2. **痛点量化达标**：现用 Google AI Studio Flow，每周浪费 ≥3 小时，且频繁"不让出图"（审核/额度/网络三件套）
3. **时代变量**：生图 API 价格已崩，"贵"这个停摆原因自动消失
4. **差异化成立**：会员站（即梦/Liblib）卷的是"生图"这 20%，"生图之后的工作流编排"（流程割裂、数据封闭、不可编程）是它们的结构盲区
5. **明令不拼便宜**：便宜是死路，拼不过平台价格战；目标是"自己离不开的工具"

## 三、产品定位（2.0）

**不是通用无限画布，是垂直工作流画布**——为园艺电商视觉场景预设的 AI 流水线工具。

核心交互模型（来自 Liblib 工作流的启发 + 用户口述确认）：
- **参数化联动**：改上游输入（换图/改关键词）→ 下游自动标"待重跑"（脏标记）
- **分段执行**：跑一段、看一段、满意再跑下一段；控制感在用户手里
- **选中运行**：框选节点 → 只运行选中的那一段
- **连线保留但默认连好**：模板自动布局，拖动只是调布局，不是从零搭建

流水线骨架（完整版 5 步，第一版只做前 2 步验证）：
```
① 输入产品图 → ② 换风格 → ③ 生成复现 JSON → ④ 3D 复现 → ⑤ 场景合成/出图
```

## 四、UI 决策（用户已拍板）

| 决策项 | 结论 |
|---|---|
| 风格 | **温馨园艺风**：暖米白底 + 鼠尾草绿点缀 + 大圆角 + 柔和暖阴影；深色为暖棕黑。否决玻璃拟态、冷色技术风 |
| 卡片形态 | **图即卡片**：大图占满，标签悬浮左上（含状态点），操作按钮悬浮右上（悬停出现）。参考即梦智能画布 |
| 上下文悬浮 | 选中卡片时，操作条贴卡片**上沿**、指令面板贴卡片**下沿**浮现，跟位；取消选中一起收起 |
| 指令面板 | 参考/标记/风格 标签页 + 参考图缩略 + 输入框 + 模型参数 chip + 圆形发送钮 |
| 运行状态 | **流光连线**（绿色流动虚线+光晕）+ 卡片扫描光动画，不要文字日志 |
| 状态点 | 极简小圆点：灰未跑 / 绿脉冲运行中 / 深绿完成 / 橙待重跑 / 红失败 |
| 布局 | 沿用现有骨架：顶部极简栏、左侧历史图库、中央画布 |
| 节点信息 | 图、关键词、模型名、比例、分辨率 |
| 红线 | 无 emoji 图标（全 SVG 描边）、无紫粉渐变、无模板味文案 |

**已交付**：高保真可交互原型 `prototypes/ui-v2.html`（迭代 3 版：深色技术风 → 温馨风 → 即梦式卡片 + 上下文悬浮）。

## 五、架构决策

**复用**：backend 全部（UnifiedAPIRouter 多供应商兼容/错误分层/本地保存）、PipelineEngine、供应商管理、.icproj 项目格式、历史图库。

**砍掉**：自由画布从零搭建的交互负担、七种卡片通用体系、激光切割等花活。

**核心改造（只有一处）**：执行模型从"手动各跑各的"改为"依赖联动"——
```
执行引擎 = 步骤列表 + 脏标记 + 分段执行
节点系统 = 注册式（CardRegistry 思路）：每类节点一个定义对象，
          第一版内置 2 个，以后加节点 = 注册新定义，不改核心
```

**技术栈不变**：pywebview + TypeScript/Vite。不换 Electron、不碰 Tauri。

**已砍功能（用户拍板）**：自动切供应商容错（失败就节点红点，手动处理）、增量重跑缓存（第一版全量重跑）、移动端、全面测试（只补序列化+容错够用就行）。

## 六、第二版预留（第一版不做，架构留口子）

- 对话 Agent：语言指挥画布（"换成侘寂风"→自动改参标脏），不是独立聊天窗
- 自建节点/连线：空白处右键加节点、拖端口连新线
- 更多节点：3D 复现、场景合成、抠图、扩图（按上班真实需求长出来）

## 七、下一步

1. 设计定稿：基于 `prototypes/ui-v2.html` 精修（用户终审）
2. 第一版开发：画布 + 2 步闭环（输入图→换风格）+ 改参联动 + 分段执行
3. 验收标准（唯一）：**上周出不了的那几张图，用它能不能出来；用户每周愿意主动打开几次**
