# Security Policy

## Reporting a Vulnerability

Please open a private report or contact the maintainer before publicly disclosing security issues.

## Secrets

Never commit API keys, provider tokens, local project files, or personal file paths.

Runtime files such as `providers_data.json`, `settings.json`, and `prompts_library.json` are ignored by Git. Example templates are provided without credentials.
