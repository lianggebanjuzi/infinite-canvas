# 4.0：Infinite Atelier 功能复刻总控执行规范

> 用途：这是后续编码 Agent 的总控文件。先读本文件，再只选择一个分卷执行；不可把多个分卷混成一次大改。
>
> 参考目标：复刻 Infinite Atelier 的**可用功能与工作方式**，不复制其源码、React 组件、品牌、提示词文本、封面图、MONOFORM 资产、文案或浏览器存储方案。
>
> 基线：`3.0-Infinite Atelier 工作流重构实施规范.md` 已完成。现有 TypeScript + PyWebView + Python + `.icproj` + 本地文件持久化继续是唯一架构基础。

---

## 1. 本轮范围与硬排除

### 1.1 必须补齐的功能簇

| 分卷 | 功能簇 | 交付结果 |
|---|---|---|
| `4.1` | 图片编辑、画布效率、画布导出 | 裁剪、蒙版局改、切图、多角度、@素材、小地图、导出 |
| `4.2` | 视频与音频 | 视频/音频节点、生成、预览、资产/历史/项目闭环 |
| `4.3` | 提示词、资产、项目、配置 | 视觉提示词库、备份恢复、最近项目、模型能力扩展 |
| `4.4` | 导演台 | 独立 3D 分镜预演工作台，并与项目资源互通 |

### 1.2 硬排除：高清放大

**不实现**高清放大、超分辨率、`super resolve` 或任何看似同义的入口。原因是当前供应商与模型能力无法可靠支撑。

- 不在操作条、右键、更多菜单、设置或提示词中留下“即将支持”的假按钮。
- 不以“重新生成更大尺寸”伪装成高清放大。
- 若未来供应商能力确认，再单独写 `4.x` 规格；不修改本系列既定验收。

### 1.3 非目标

- 不迁 React、不引入浏览器作为项目事实源、不把 API Key 写入 `.icproj`。
- 不让视频、音频、导演台阻塞已有图片主链路。
- 不为复刻功能删除已有扩图、批次、历史、资产、工作流模板或 3.0 连线语义。
- 不把“功能存在”定义成“菜单里有按钮”；每项必须有输入、运行、结果、保存、失败与恢复路径。

---

## 2. 现状与参考功能映射

### 2.1 已完成，不重复造轮子

- 图片/文本节点、局部显式上下文、参考图、文生图/图生图、多候选、批次与重试。
- 图片继续创作、扩图、画布连线语义、结果追溯、项目保存、资产/历史抽屉、工作流模板。
- 多供应商、多 Key、图片/文本/视频模型分类；视频后端已有基础异步协议。
- 项目主页的“新建 / 打开 / 返回画布”基础入口。

### 2.2 要补的是产品闭环而非按钮

| 参考能力 | 当前缺口 | 所属分卷 |
|---|---|---|
| 裁剪、蒙版编辑、切图、多角度 | 没有完整用户入口与可追溯结果链 | 4.1 |
| 小地图、@资源、画布资源导出 | 没有或不完整 | 4.1 |
| 视频/音频节点 | 视频后端未接前端；音频未接入 | 4.2 |
| 图文提示词卡、分类、封面管理 | 只有文本收藏 | 4.3 |
| 资产/项目整包备份恢复 | `.icproj` 不能独立覆盖所有资产与全局资料 | 4.3 |
| 最近项目、项目管理、主题体系 | 主页仅有基础入口 | 4.3 |
| 可扩展模型能力与参数 schema | 现有配置可用，但无法安全表达更多媒体能力 | 4.3 |
| 3D 导演预演 | 完全没有 | 4.4 |

---

## 3. 所有分卷必须遵守的共享契约

### 3.1 本地项目是唯一事实源

1. 画布状态仍由 `FlowNode` / `FlowEdge` / `GenerationTrace` 承载。
2. 项目保存仍走 `persistence.ts` 与 `backend/api/project_api.py`；新字段必须可选、旧 `.icproj` 可读。
3. 大文件必须落盘并保存路径/缩略图/元数据，不得长期把视频或大图 base64 塞进节点 JSON。
4. 每次新功能生成出的结果必须能在：画布、历史、资产（用户保存后）、重新打开项目后被找到。

### 3.2 媒体任务状态机

所有远端媒体任务（图片局改、角度、视频、音频）统一使用：

```text
local task 创建
  → queued（本地限流等待）
  → submitting
  → accepted（拿到 remote_task_id）
  → queued / processing（远端中间态）
  → succeeded | failed | cancelled | uncertain
```

- 收到 `202 + remote_task_id` 后，只查询原任务，禁止自动换 Key 重投。
- `remote_task_id`、模型、供应商、输入来源、提交时间写入 trace 与任务状态。
- 用户能看到“排队 / 已提交 / 查询恢复中 / 远端失败”的区别；不得把查询错误显示成“所有 Key 失败”。
- 任务失败必须保留源节点和用户输入；“重试”创建新本地任务，旧任务不覆盖。

### 3.3 节点、连线与来源

- 新媒体节点必须声明端口类型；禁止放宽现有 `port-types.ts` 的校验来“先跑起来”。
- 自动产生的结果放在来源右侧并用 `result` 连线；用户手动添加的图片/音频/视频为 `reference`。
- 生成只消费直接相连或显式 @引用的上下文，绝不扫描整张画布猜素材。
- 每个结果 trace 至少记录 `outputType`、`prompt`、`model`、`references`、`createdAt`；本地编辑还要记录操作参数。

### 3.4 能力门控

- 配置存在不代表功能可用。前端只展示当前模型明确支持的动作。
- 对“未知模型 / 未配置端点 / 不支持 mask、video、audio”的情况，隐藏动作或展示明确禁用理由；不可提交必失败请求。
- `model-config.ts` / 后端模型规则是唯一能力来源；UI 不写关键字判断副本。

### 3.5 不泄露敏感信息

- API Key、Authorization、完整代理 URL 不写入 trace、历史、导出包、控制台或前端错误。
- 默认备份不含 Key。若未来要迁移 Key，必须独立、显式、加密并由用户确认；不属于本系列首期。

---

## 4. 推荐执行顺序与停止点

```text
4.1-A 本地裁剪 / 切图 / 小地图
  → 4.1-B 蒙版局改 / 多角度 / @资源 / 导出
  → 4.2-A 视频节点（复用已存在后端）
  → 4.2-B 音频节点与媒体资产
  → 4.3-A 提示词与资产备份
  → 4.3-B 项目主页与模型 schema
  → 4.4 导演台（独立项目式开发）
```

每个箭头都是停止点：上一项没有通过人工验收、`npm run typecheck`、`npm run build`，不得进入下一项。4.4 可与 4.3-B 并行规划，但不与主画布共用未稳定的数据模型。

---

## 5. 每个 Agent 回合的交付格式

每次只完成一个小切片，并报告：

1. 对应文档、章节、验收编号。
2. 改动文件与数据兼容变化。
3. 执行过的命令及结果：至少 `npm run typecheck`；改入口/样式/构建加 `npm run build`；改 Python 加 `python -m py_compile`。
4. 人工验证的具体路径和截图/日志要点。
5. 尚未完成项、已知限制、是否涉及真实媒体费用。

禁止：

- 一次性重写画布或替换持久化格式。
- 为了截图好看复制参考项目图片、提示词、配色或文案。
- 在没有供应商能力的情况下伪造成功结果。
- 未经用户许可用真实 API 批量生成来“测试”。

---

## 6. 总体验收

在全部分卷完成后，用户应可完成：

```text
从项目主页打开本地项目
  → 导入图片 / 视频 / 音频 / 文本资源
  → 用提示词卡或 @引用组织上下文
  → 裁剪、切图或蒙版局改一张图，并保留来源
  → 用结果生成视频或音频，看到异步状态并播放结果
  → 把需要的素材加入资产，保存项目
  → 导出项目资源包，在另一台机器导入恢复
  → 可选地把一张图送入导演台做 3D 构图预演，再导回 PNG/MP4
```

上述过程任意失败都不丢源素材、不覆盖旧结果、不把网络异常误报为密钥问题。
