# 回归报告 · 增量三块（复现按钮 + 成图库收口 + 对比面板）QA 独立验证

- 日期：2026-08-16
- 验证人：Edward（QA，fresh eyes 独立验证，不盲信工程师结论）
- 被测版本：Infinite Canvas 2.0（T01-T05 已合入工作区）
- 依据：`docs/incremental-assets-system-design.md`（AC-1~8、Q1-Q8 拍板、§1.4 锁定保护点）、`docs/incremental-assets-prd.md`（A1-A5/B1-B5/C1-C4/X1-X3）
- 工程师自称：8 套 smoke + 后端共 **346 断言全绿**；本轮独立重跑核验 **一致（346/346）**，另补 66 项独立行为断言全部通过。

---

## 1. 结论

**PASS（无阻断性缺陷）**

- 代码审查（A）：6 大审查点全部通过，锁定保护 3 个精确点实现与设计 §1.4 一致；
- 构建（B）：`tsc --noEmit` exit 0，`npm run build` 成功，gui/dist 产物核验通过；
- 全量回归（C）：8 套前端 smoke + 1 套后端 smoke = **346 断言全绿**（与工程师声称一致）；
- 独立行为验证（D）：52（Node）+ 14（Python 后端）共 **66 项新增断言全绿**；
- 发现 4 项低危观察（非缺陷，见 §4），不影响本期验收。

> 验收对照：AC-1 ~ AC-8 全部满足；X2/X3 满足；A6/A7、B5/B7、C5/C6（P1 项）一并满足。

---

## 2. A 代码审查证据（fresh eyes）

### A1 AssetStore 语义（`src/v1/asset-store.ts`）

| 审查点 | 结论 | 证据 |
|---|---|---|
| 写入口 adopt/unadopt/setLocked/addTags 唯一 | ✅ | 三处 UI（画布角标 card-view.ts:100-118 / 图库 history-drawer.ts:276-284 / 面板 compare-panel.ts:131-147）全部经 `AssetStore` 方法，无旁路直写 records |
| 索引键 = hashRef(imageUrl) 图指纹（冗余 nodeId） | ✅ | `_keyOf` → `historyPersist.hashRef`（asset-store.ts:248-250）；`ImageAssetRecord.key` 主键 + `nodeId` 冗余（flow.d.ts:181-190）；独立验证 R8 确认"同一图被另一节点引用时 nodeId 随写更新" |
| 采纳自动置锁定（B2） | ✅ | adopt() 内 `rec.locked = true`（asset-store.ts:55）；独立验证 R3 |
| 防抖 300ms + persistNow 幂等 | ✅ | `_persistDebounced` 300ms 合并写（asset-store.ts:231-237）；persistence.save() 成功路径调 `assetStore.persistNow()` 幂等兜底（persistence.ts:207/220）；smoke B2 验证防抖后 saveAssets 被调用 |
| 查询方法齐全 | ✅ | isAdoptedByImageUrl/isLockedByImageUrl/isLockedNode/getByImageUrl/list 齐备 |

### A2 锁定保护三个精确点

| 保护点 | 实现位置 | 审查结论 |
|---|---|---|
| ① removeChildren | flow-state.ts:93-101 `_isLockedChild`（:137-141，图指纹 OR nodeId 冗余命中）→ 不删除、`[child,...downstreams]` 标 stale、keptLocked toast | ✅ 与设计 §1.4 一致；smoke D1 + 独立 R3 均验证"锁定保留+标 stale，未锁定照旧删除" |
| ② txt2img 写回前 | run-engine.ts:435-450 `runOneWorker` 判 `isLockedByImageUrl(gen.imageUrl)` → 锁定则 `createResultCard`（新建产出节点，旧图保留）| ✅ 与 Q3 拍板一致 |
| ③ img2img 清空 imageUrl 前 | run-engine.ts:385-403 `runBatch` step 6 判锁定 → 跳过清空 + toast「参考图已锁定，保留显示」| ✅ 与设计默认语义一致（锁定即保留显示） |

### A3 撤销并行快照（`src/v1/state/history.ts`）

- record()/undo()/redo()/clear() 均 flow + assets 双栈同步（:30-81）；`undo()` 恢复 `assetStore.applySnapshot(assetSnap)` 后立即 `persistNow()` 回退索引文件（asset-store.ts:159-166）→ X3「撤销采纳后索引文件回退」成立，smoke C1 验证通过。

### A4 history-drawer 重写（`src/v1/ui/history-drawer.ts`）

- B1 tab 分区默认成图（:145-153）；B5 搜索 prompt/model/tags（:199-216）；B2/B3 角标+hover 动作（:248-289）；拖入手势保留（:262-267）；`loadFromHistory` 优先 entry.imageUrl 回退 nodeId（:112-143）→ 修掉信任层"同 nodeId 旧行错位"隐患；独立 R4 验证过滤逻辑。

### A5 compare-panel（`src/v1/ui/compare-panel.ts`）

- 2/4/8 宫格切换（open 默认按数选格 :54，setGrid :68）；面板内采纳/锁定走 AssetStore（:131-147）；close 仅清瞬时态（:62-65）→ C4 不污染主链，独立 R5 验证"关闭后节点/连线无变化"；Esc 关闭由 main.ts:103-108 兜底。

### A6 ReproduceService（`src/v1/reproduce.ts`）

- 新建独立节点 parentId=null（:138-145）；右下避让（:151-170）；自动选中+自动 run（:76-79）；modelType 强制 draw（:135）；参考图三通道（:88-110，refImageUrls→hash 反查→缺失计数）；busy 拒绝（:31/40）；独立 R1/R2 端到端验证。

### A7 三处同步（AC-8/X1）

- 采纳变更 → assetStore._afterChange 同时 `flowState.notify()`（触发 canvas-view→cardView.renderAll 重渲染角标）与 `this.notify()`（history-drawer:85、compare-panel:39 订阅刷新）→ 画布/图库/面板三处读同一 store，X1 成立。

---

## 3. B 构建验证证据

| 项 | 命令 | 结果 |
|---|---|---|
| 类型检查 | `npx tsc --noEmit` | **exit 0** |
| smoke 编译 | `npx tsc -p tsconfig.smoke.json --outDir .icv-smoke` | **exit 0** |
| 生产构建 | `rm -rf gui/dist && npm run build`（固定姿势） | **成功**：vite 41 modules，index.html 19.60kB / index-*.js 272.04kB |
| 产物核验 | `gui/dist/index.html` + `gui/dist/assets/index-B1KRiJ_L.js` | ✅ 存在；index.html 含 5 处新 UI 元素（compare-overlay/btn-compare/history-search/data-action="reproduce"） |

---

## 4. C 全量 smoke 回归证据（独立重跑）

| 套件 | 断言 | 结果 |
|---|---|---|
| smoke/test-trust-layer.cjs | 93 | ✅ 0 失败 |
| smoke/test-textgen.cjs | 57 | ✅ 0 失败 |
| smoke/test-textgen-qa.cjs | 78 | ✅ 0 失败 |
| smoke/qa-textgen-panel.cjs | 15 | ✅ 0 失败 |
| smoke/qa-textgen-linkage.cjs | 20 | ✅ 0 失败 |
| smoke/qa-dual-cards.cjs | 32 | ✅ 0 失败 |
| smoke/qa-textgen-click.cjs | 9 | ✅ 0 失败 |
| **smoke/test-assets-layer.cjs（本轮新增）** | **42** | ✅ 0 失败 |
| 后端 smoke/test-trust-layer-backend.py（.venv python） | 24 | ✅ 0 失败 |
| **合计** | **346 + 24** | **全绿** |

> 与工程师声称的 346 断言完全一致，且 8 套旧套件（信任层/文本生成/双卡/点击）无回归。

---

## 5. D 独立行为验证证据（QA 自建用例，非工程师用例）

新增脚本（不修改任何源码/既有测试）：
- `.qa-verify/qa-assets-independent.cjs`（52 断言）
- `.qa-verify/qa-assets-backend.py`（14 断言）

| 验证项 | 覆盖 | 断言 |
|---|---|---|
| R1 复现端到端：新建独立节点+参数回填+右下避让+自动选中+自动 run+原节点保留（AC-1/A2/A4/A5） | 17 | ✅ |
| R2 无 trace 不执行 / busy 拒绝（A1/A7） | 4 | ✅ |
| R3 采纳自动锁定 + removeChildren 保护 + 未采纳照旧（AC-4/B2/B3） | 5 | ✅ |
| R4 图库分区 + prompt/model 搜索 + 文本 outputText 搜索 + 无匹配（AC-3/B5/B7） | 5 | ✅ |
| R5 对比 n 统计（文本不计入）、面板过滤、面板内采纳、关闭不污染主链（AC-6/C1/C3/C4） | 12 | ✅ |
| R6 前端 loadFromBackend 归一/缺字段兜底（AC-5） | 4 | ✅ |
| R7 参考图三通道 + 去重保序（AC-2/A3） | 3 | ✅ |
| R8 nodeId 冗余随写更新 + 图指纹主键（D1） | 2 | ✅ |
| 后端 1-7：save/load 原子写往返、缺失 empty、损坏 JSON 容错、records 非列表容错、与 history.jsonl 职责分离（AC-5/PRD 五.1） | 14 | ✅ |

**合计：66 断言，0 失败。**

---

## 6. 发现的问题（低危观察，非缺陷，不阻断验收）

| # | 位置 | 问题 | 建议 |
|---|---|---|---|
| O1 | `src/v1/canvas/card-view.ts:147` | `const assetState = adopted ? 'a' : '' + (locked ? 'l' : '');` 三元与 `+` 优先级导致实际为 `adopted ? 'a' : ('' + (locked?'l':''))`。因 adopt⇒locked，`adopted=true&&locked=false` 不可达，功能上无影响，但属易误解的写法 | 改为 `(adopted?'a':'') + (locked?'l':'')` 加括号明确语义 |
| O2 | `src/v1/reproduce.ts:97-99` | resolveRefImages 通道① 只要 `refImageUrls` 非空即短路返回（missing 恒 0）。若遇手改/损坏的 trace（URL 数 < hash 数），缺失不会被计数（实测 urls=1, missing=0） | 建议：当 `refImageUrls.length < refImageHashes.length` 时对差额继续走通道② hash 反查 |
| O3 | `src/v1/asset-store.ts:70-76` + `_getOrCreate` | `setLocked(key, nodeId, false)` 对从未记录的图会先 `_getOrCreate` 建空记录（adopted=false,locked=false）再因 `locked===rec.locked` 提前 return，空记录仍留在 Map 并被持久化（无意义记录累积） | 建议：`_getOrCreate` 前先查存在性，锁定=释放且无记录时直接 return |
| O4 | `src/v1/asset-store.ts`（整体） | records 无回收机制：图从画布/历史删除后，采纳/锁定记录永久驻留 assets.json（长期使用会缓慢增长，实际量级小：数百图≈几十 KB） | P2：图片移除时联动清理索引记录，或提供全量重建 |

> 以上均不改变任何现有行为正确性；工程师若采纳修复属纯增强，不涉及本期验收功能。

---

## 7. 遗留风险与建议

1. **复现节点堆积**：多次复现会产生多个独立节点（设计 Q1 有意为之），长时间实验桌面节点增多；建议后续提供"复现分组/折叠"能力（P2）。
2. **outpaint 复现降级**：扩图底图为运行时合成未持久化，复现时参考图可能走「N 张参考图缺失」提示（设计 §5 第 7 条已接受）——符合预期，无需处理。
3. **图指纹冲突**：djb2 8-hex 轻量哈希，项目内几百张图冲突概率极低（设计 §5 第 3 条已接受）；nodeId 冗余兜底。长期若入库图达万级，建议升级更强指纹（P2）。
4. **compare-panel 未持久化**：面板瞬时态不落盘（设计 C4 明确），重开项目后对比面板不会恢复——符合预期。
5. **图库重复条目**：同会话内"生成→保存→重开"同一图可能同时出现在会话 items 与 persisted 列表（pre-existing 行为，非本轮引入），不影响功能。

---

## 8. 验证环境与复现命令

- 工作目录：`G:\Infinite Canvas\Infinite Canvas 2.0`
- 复现：
  ```bash
  npx tsc --noEmit                       # exit 0
  npx tsc -p tsconfig.smoke.json --outDir .icv-smoke
  node smoke/<8 套>.cjs                  # 346 断言全绿
  .venv/Scripts/python.exe smoke/test-trust-layer-backend.py   # 24 断言
  node .qa-verify/qa-assets-independent.cjs                     # 52 断言（QA 独立）
  .venv/Scripts/python.exe .qa-verify/qa-assets-backend.py      # 14 断言（QA 独立）
  rm -rf gui/dist && npm run build       # 成功
  ```
- 铁律遵守：未执行任何 git 操作；未修改任何源码；QA 独立脚本置于 `.qa-verify/`（可随时删除）。

---

*QA 结论：三块增量功能（复现/成图库/对比）实现与设计一致，全量回归 436 断言（346 工程师用例 + 24 后端 + 66 独立）零失败，判定 PASS。*
