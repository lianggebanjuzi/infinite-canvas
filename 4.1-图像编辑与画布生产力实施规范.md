# 4.1：图像编辑与画布生产力实施规范

> 前置：4.0 总控；3.0 工作流规范已完成。
>
> 目标：把图片节点从“只能生成/扩图”升级为可直接编辑、拆分、引用和导出的创作材料。**不包含高清放大。**

## 1. 交付边界

| 切片 | 必做功能 | 结果类型 |
|---|---|---|
| A | 本地裁剪、图片切图、自由缩放锁定、小地图 | 不消耗 API 的派生素材 |
| B | 手绘蒙版局部修改、图像多角度、@素材引用 | 远端图片任务，产生可追溯结果 |
| C | 画布/选中节点导出与导入 | 可移植 `.icbundle` ZIP |

不做：高清放大、超分、背景移除、任何未被已配置模型确认支持的图像工具。

## 2. 数据与节点契约

### 2.1 新字段必须可选

在 `src/v1/types/flow.d.ts` 给 `FlowNode` 或 `params` 增加可选字段；旧项目缺失时按原样显示。

```ts
type ImageEditKind = 'crop' | 'split' | 'mask-edit' | 'angle';

interface ImageEditTrace extends GenerationTrace {
  outputType: 'image-edit';
  editKind: ImageEditKind;
  sourceNodeId: string;
  crop?: { x: number; y: number; width: number; height: number; rotation?: number };
  split?: { rows: number; cols: number; index: number };
  mask?: { path?: string; width: number; height: number }; // 仅本地路径或压缩 PNG，不能长期塞大 base64
  angle?: { preset: string; instruction: string };
}
```

建议优先复用 `image-gen` 结果节点 + `trace.outputType='image-edit'`，除非现有节点渲染无法表达多块本地输出。不要为每一种操作造新的画布实体。

### 2.2 来源规则

- 裁剪/切图：源图片 → 派生素材，自动 `result` 连线；源图不改写。
- 蒙版局改/多角度：源图与操作节点 → 新结果，自动 `result` 连线；输入图作为 `reference` 写入 trace。
- 一次切出多块时，创建一组结果；每块有自己的 split index 和来源。
- 删除结果不得删除源图；删除源图时按已有连线规则提示，不静默级联删除。

## 3. 切片 A：本地编辑与导航

### A1. 裁剪

**入口**：选中图片节点的操作条、右键菜单、大图查看器；三个入口调用同一函数。

**行为**：

1. 打开模态编辑器，加载本地原图；无原图时从当前可访问 URL 下载一份到项目资源目录。
2. 支持拖拽裁剪框、比例预设（自由、1:1、3:4、4:3、16:9、9:16）、旋转 90°、重置、取消、确认。
3. 确认后浏览器以 Canvas 或后端 Pillow 生成新文件；原图与裁剪参数写入 trace。
4. 新图放在源图右侧，作为“裁剪结果”，自动选中但不自动发起生成。

**验收 A1**：裁剪后关闭重开项目，源图、裁剪结果、连线、比例和查看大图均存在；撤销/重做不会破坏本地文件引用。

### A2. 图片切图

**入口**：图片操作条的“切图”。

**首期能力**：2×2、3×3、横向 2/3/4、纵向 2/3/4；不做自动识别漫画格。

**行为**：预览网格 → 用户选择行列与是否保留边缘余量 → 输出每个格子为独立本地图片结果。必须按从左到右、从上到下稳定排序，并记录 row/column/index。

**验收 A2**：3×3 输出 9 个可单独拖拽、加入资产、继续创作的节点；批量导出时文件名不重复。

### A3. 尺寸锁定与小地图

- 图片节点显示“锁定比例 / 自由缩放”状态；比例锁定默认开启。
- 自由缩放只改变节点布局尺寸，不改原图像素，不生成新资源。
- 增加 Canvas minimap：显示节点边界、当前位置视窗、点击/拖动跳转；不显示缩略大图以免性能崩坏。
- 小地图默认可收起，节点超过 12 个或画布跨度超过 2 个屏幕时自动建议打开，但不可强制。

**验收 A3**：100 个节点时拖动画布与小地图仍可用；缩放/平移/项目恢复后小地图位置正确。

## 4. 切片 B：远端图片编辑

### B1. 蒙版局部修改

**前置能力**：模型必须明确标记 `imageEdit.mask=true`；未满足时不展示入口。当前后端已有部分 mask/outpaint 基础，必须收敛到 Unified API，不允许页面直接请求供应商。

**交互**：

1. 打开编辑器，显示原图、笔刷大小、橡皮、撤销/重做、半透明蒙版、缩放、清空。
2. 白色区域为“允许修改”，黑色/透明区域为“保留”。提交前显示将发送的图片、mask、提示词、模型。
3. 点击生成后立即创建本地任务与下游占位节点；接到远端 task id 后按 4.0 状态机轮询。
4. 成功结果生成新节点；失败时保留蒙版和提示词，可修改后新建重试。

**后端要求**：新增统一 `image-edit` 请求构建器，按模型 adapter 选择 Gemini 原生或 OpenAI images edits；禁止把所有模型强塞一个 payload。mask 文件使用临时目录/项目资源目录，任务完成或取消后延迟清理。

**验收 B1**：局改成功、远端 failed、轮询断网、用户取消四种情形均不重发第二个远端任务；错误显示 remote task id。

### B2. 多角度 / 视角变化

这不是“自动转 3D”。首期定义为：对一张源图加上明确视角预设/自定义文字，调用支持图像参考的模型生成一张新视角图片。

- 预设：正面、左 45°、右 45°、侧面、俯视、仰视、背面；每个预设只生成自己的结构化提示前缀。
- 自定义说明：允许补充“保持人物服装、环境和主体一致”等文字。
- 前端必须提示“生成式变化，不保证几何完全一致”。
- 结果 trace 写入预设、完整实际 prompt、源图 fingerprint、模型与任务 id。

**验收 B2**：没有图片参考能力的模型不显示；结果可继续创作，且来源卡和视角动作在详情中可见。

### B3. @素材引用

**目的**：让用户在 prompt 中显式选择资产，而不是依赖看不懂的连线。

- 输入 `@` 打开资源选择器，支持搜索当前项目资产、历史图片、画布图片、文本片段。
- 插入显示为不可编辑 token；删除 token 时同步移除引用。
- 保存结构化 `mentions[]`，生成前解析为 `referenceImages` / 附加文本；不要只保存 token 的可见字符串。
- 资源丢失时 token 标红，提交前阻止并提示替换/移除。

**验收 B3**：同名素材不会引用错；复制 prompt 到纯文本会降级为可读名称；项目恢复后 token 仍绑定原资源。

## 5. 切片 C：导出与导入

### C1. `.icbundle` 格式

ZIP 内容：

```text
manifest.json       # schemaVersion、导出时间、应用版本、内容清单
project.icproj      # 画布与 trace；不含 API Key
assets/<sha>.<ext>  # 画布实际依赖的图片/视频/音频
thumbs/<sha>.webp   # 可选缩略图
```

- 资源用 SHA-256 或稳定 fingerprint 去重；manifest 记录原始逻辑 ID 到 bundle 路径映射。
- “导出选中节点”只打包所选节点、向上游追溯到直接依赖的必要资源与连线。
- “导出当前项目”打包当前项目所有节点和引用资源。

### C2. 导入

- 导入前验证 manifest、版本、大小、路径穿越、重复文件与 MIME；不信任 ZIP 中任何文件名。
- 冲突策略：新建项目导入（默认）或插入当前画布（仅选中节点包可用）。
- 导入失败必须原子回滚，不能留下半份项目或临时文件。

**验收 C**：离线导出→删除本地原文件→导入包后图像/视频仍能预览；bundle 中搜索不到 API Key。

## 6. 代码地图与最低验证

| 领域 | 优先文件 |
|---|---|
| 类型/迁移 | `src/v1/types/flow.d.ts`、`src/v1/persistence.ts` |
| 画布工具 | `src/v1/ui/action-bar.ts`、`src/v1/canvas/card-view.ts`、`src/v1/canvas/interactions.ts` |
| 编辑器 | 新建 `src/v1/ui/image-editor/*`，不要塞进 `card-view.ts` |
| 任务与 adapter | `backend/api/unified_api.py`、`backend/api/image_api.py`、`src/v1/engine/run-engine.ts` |
| 导出 | 新建 `backend/api/bundle_api.py` + `src/v1/ui/export-*` |

每个切片必须执行 `npm run typecheck`、`npm run build`；改 Python 时执行 `python -m py_compile`；不使用真实付费 API 的 mock 验证任务 accepted 后不重投。
