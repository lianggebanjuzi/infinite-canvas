# 4.0 Infinite Atelier 功能复刻 - 审批意见

> 审批日期：2026-08-28  
> 审批对象：`docs/4.0-交付总结-2026-08-28.md` 及当前未提交工作区  
> 结论：**暂不通过，退回修复后复审**

## 审批结论

本次交付的主要构建与 smoke 门禁均已复跑通过，但存在 1 项 P1 和 1 项 P2。交付总结中“P0=0、P1=0、可交付”的结论目前不成立；完成下列问题修复、补充回归测试并更新 QA 报告后，可重新提交审批。

## 阻塞项

| 级别 | 问题 | 证据与影响 | 准入要求 |
|---|---|---|---|
| P1 | `director_save_project` 未校验保存目标和工程格式 | `backend/director_api.py:52-60` 直接对调用方传入的任意 `path` 和 `data` 执行 `atomic_write_json`。动态复现中，保存到 `not-a-director.json` 返回 `success` 且文件被写入。该接口可覆盖非 `.icdirector` 文件，也可写入不含 `format: 'icdirector'` 的内容。QA 报告声称“拒绝非 icdirector”，但 `smoke/director_smoke.py:134-140` 仅验证了读取非法文件时的拒绝，并未覆盖保存接口。 | `director_save_project` 与 `director_save_project_as` 都必须校验目标扩展名为 `.icdirector`，并校验工程数据为合法对象且 `format === 'icdirector'`；拒绝时不得创建或覆盖文件。补充成功、非法扩展名、非法格式、既有文件不被覆盖四类测试。 |

## 非阻塞项

| 级别 | 问题 | 证据与影响 | 修复要求 |
|---|---|---|---|
| P2 | `.icbundle` 导入取消或最终写入失败后留下孤立资产 | `backend/api/bundle_api.py:274-281` 在选择新项目路径前已将资源复制至资产目录；取消发生在 `:297-299`，没有清理刚复制的资源。动态复现：导入返回 `cancelled` 后，资产目录仍保留新复制的哈希命名 `.png`。这与模块注释及 QA 报告中的“原子回滚/失败自动清理”不符。 | 记录本次导入新建的资产；在取消、目标项目写入失败或后续异常时删除这些新建资产，且不得删除导入前已存在的去重资产。补充取消、项目写入失败和既有去重资产保留的回归测试。 |

## 已复验项

以下命令已在当前工作区独立复跑：

- `npm run typecheck`：通过。
- `npm run build`：主应用与导演台 bundle 均成功生成。
- Python 编译：使用 PowerShell 展开的显式 Python 文件列表执行 `python -m py_compile`，通过。
- `python smoke/director_smoke.py`：8/8 通过。
- `python smoke/qa_p2_1_check_path.py`：5/5 通过。
- `python smoke/qa_p2_3_delete_temp.py`：6/6 通过。

构建仍会输出既有的 `EMPTY_IMPORT_META` 警告，位置为 `src/director/export/mp4.ts:44`；当前不作为本次退回原因，但应继续保留在遗留项中。

## 复审准入

1. 修复 P1 和 P2，并为两项补充可重复运行的 smoke 或单元测试。
2. 重新执行并记录 typecheck、双 bundle 构建、Python 编译及相关 smoke。
3. 将 `docs/4.0-QA验收报告-2026-08-28.md` 和交付总结中的 P0/P1 结论改为与实际验证一致的表述。
4. 保持当前未提交改动范围不扩大；复审只确认上述缺陷修复未引入回归。

## 2026-08-29 UI 复查补充

此前基于构建和 smoke 的“通过”口头结论撤回。本节基于实际页面状态、代码路径和最小运行复现补充；以下问题均未包含在原交付总结的验收范围内，当前结论仍为 **暂不通过**。

| 级别 | 问题 | 证据与影响 | 准入要求 |
|---|---|---|---|
| P1 | 音频生成在发请求前必然失败 | `backend/api/audio_api.py:243` 调用 `_generate_audio_inner` 时未传入 `connection`；该方法在 `:249-250` 直接读取未定义变量。最小调用复现为 `NameError: name 'connection' is not defined`。任何已配置音频模型都无法生成。 | 显式传入并使用 `connection`，补充成功创建请求、连接缺失、任务失败三类自动化回归。 |
| P1 | 导演台回传 MP4 不能按本地媒体地址播放 | `src/v1/ui/director-bridge.ts:179-180,219-220` 将 Windows 文件路径同时写入 `video.url` 和历史 `videoUrl`；查看器在 `src/v1/ui/video-viewer.ts:28,42` 会把该非空字符串直接作为 `<video src>`，不会转换为 `file:///...`。导演台即使打开成功，回传视频仍可能无法播放、下载或在资产库复用。 | 统一通过同一个路径转 URL 函数写入节点、历史和资产记录；补充导演台 MP4 回传后的播放、下载、历史恢复回归。 |
| P1 | 自定义能力 schema 只改变前端门控，未接入真实生成路由 | 前端 `src/v1/nodes/model-config.ts:81-89` 会把 schema 当作可用能力；后端运行时的 `backend/api/unified_api.py:1139-1147` 只按模型名规则选择协议，未读取 schema 的 `requestAdapter` / 字段映射。`backend/api/provider_api.py:739-747` 也明确说明“生成测试”只返回预览、不触发真实生成。设置页因此会让用户以为自定义模型已可运行，实际请求不会按其配置发送。 | 在后端统一路由中消费经校验后的 schema，或在完全接通前隐藏“可运行”状态和相关入口；补充自定义图像、视频、音频 schema 的端到端回归。 |
| P2 | 视频文件丢失后的恢复入口不可达 | `src/v1/ui/video-viewer.ts:28` 只要 `originalPath` 有值就合成一个 `file://` URL，`:30-39` 的恢复区便永不渲染；文件已移动或删除时只会得到失效的播放器，无法点到“重新定位文件/从历史恢复”。 | 监听视频加载错误后切换到恢复态，并补充文件不存在、重新定位、历史恢复的 UI 回归。 |
| P2 | 手动导入视频使用音频探测器，媒体元数据会被误标 | `backend/api/image_api.py:197-200` 对所有媒体导入调用 `_probe_audio_metadata`。例如 `.mp4` 可能被标为 `audio/mp4`，视频的尺寸/正确 MIME 也不会被提取，影响资产库和历史展示。 | 按 `kind` 分流音频与视频元数据探测，并覆盖 MP4、WebM、MP3、WAV 导入。 |

### 本轮验证

- `npm run typecheck`：通过，但不能发现 Python 运行期的未定义变量。
- 最小 Python 调用：稳定复现音频生成的 `NameError`。
- 静态路径断言：确认导演台 MP4 写入原始文件路径，且视频查看器的恢复分支被非空 `originalPath` 永久绕过。
- 本轮未将“实际生成音频、实际导演台导出 MP4、丢失视频重定位”计为通过，因为当前代码已证明其路径不成立。

## 2026-08-29 修复复验

上节列出的新增问题已完成修复并独立复验。此前的“暂不通过”结论针对修复前工作区；本节只记录本轮修复后的状态，不覆盖仍待人工端到端验收的真实供应商调用和桌面窗口交互。

| 原问题 | 修复 | 复验证据 | 状态 |
|---|---|---|---|
| 音频生成 `connection` 未定义 | 已把解析出的连接显式传入 `_generate_audio_inner`。 | `python smoke/test_audio_connection_regression.py` mock 断言请求到达 HTTP 边界，且 URL、Bearer Key 均来自该连接。 | 已修复 |
| 导演台 MP4 原始路径被当 URL | 节点和历史统一写入规范 `file:///...`；兼容旧项目中的 Windows 原始路径；播放器用 DOM `.src` 设置媒体地址。 | `npm run smoke:director`、`npm run build` 通过；路径编码和旧路径兼容由前端逻辑覆盖。 | 已修复 |
| 文件丢失后无法恢复 | 改为监听视频媒体 `error` 后进入恢复态；重新定位会把选中的文件经后端落盘，再写入绝对路径和规范 URL。 | `smoke/test_schema_runtime_gate.py` 验证无 `File.path` 时 data URL 可落为绝对路径，且字节一致。 | 已修复 |
| custom-declarative schema 假可用 | 未完整接入执行器前，前端不再把它列为可运行模型，旧节点运行前拦截；后端在发网前二次拒绝；设置页明确为“仅预览/连接测试”。 | `smoke/test_schema_runtime_gate.py` 验证图像、视频请求在网络调用前被拒绝。 | 已修复（明确降级） |
| 视频导入误用音频元数据 | 视频改用独立 MIME/时长探测。 | `smoke/test_schema_runtime_gate.py` 验证 MP4 导入记录为 `video/mp4`。 | 已修复 |

### 本轮完整门禁

- `npm run build`：通过，主应用与导演台 bundle 均成功生成。
- `npm run smoke:director`：通过。
- `python smoke/test_audio_connection_regression.py`：通过。
- `python smoke/test_schema_runtime_gate.py`：通过。
- `git diff --check`：通过。
- 本地浏览器 UI 回归：节点工具栏中的“更多”已不存在；图片节点的首尾帧和含音频控件均为 `display: none`。

构建仍会输出既有 `src/director/export/mp4.ts:44` 的 `EMPTY_IMPORT_META` 警告；该警告未在本轮修复范围内，不能被当作已验证的 MP4 导出兼容性。

## 2026-08-29 交互与入口复验补充

用户在实际画布中指出了两项未覆盖到的 UI 问题。本节记录问题、修复边界和复验，不将截图中“看起来正常”替代为功能结论。

| 问题 | 修复 | 复验证据 | 状态 |
|---|---|---|---|
| 选中不具备可用动作的节点时，节点上方仍残留白色圆角空条和三条竖线 | `ActionBar.sync()` 现在按实际可见按钮决定分隔线是否显示；若不存在任何可用操作，会撤掉整个操作条的显示态。 | typecheck 通过；代码路径确保隐藏按钮后不会再留下孤立分隔线。 | 已修复，待桌面端实机回归 |
| 项目开始页为窄弹窗，主题和清理等控件像浏览器原生按钮，项目入口层级混乱 | 项目入口改为桌面双栏布局：新建、打开、最近项目为主流程；主题、备份、项目包放入辅助区。主题使用带色板的选择控件，最近项目有明确空态。原按钮 ID 和既有事件均保留；桌面桥接暂不可用时，最近项目降级为空态而不产生未处理异常。 | 本地浏览器回归：`项目` 打开入口；主题切换到“深色”后 `data-theme=dark` 且仅该选项为 active；“继续当前画布”会恢复 `hidden=true`。`npm run typecheck`、`npm run build`、`git diff --check` 通过。 | 已修复，待桌面端文件选择/备份实机回归 |

本轮仍未执行可配置供应商的真实生成请求，也未在打包后的 pywebview 窗口中完成文件选择、备份和项目包导入导出的端到端测试。因此，审批结论保持为 **待上述桌面端实机回归后再最终验收**，不得仅以构建成功标为成功验收。
