# 资产增强 · 交付总结（R2 资产配方持久化 + R3 历史批次分组）

> 日期：2026-08-18
> 交付：软件开发团队（software-asset-enhance）：许清楚(PM) → 高见远(架构) → 寇豆码(工程) → 严过关(QA)
> 代码基线：commit `0136548` → 本次 commit `1e7922f`（main 分支，本地留档未 push）

---

## 1. TL;DR

让「采纳过的图」成为**跨项目可复现、可批次对比**的资产：配方随采纳落盘 assets.json（修复跨项目复现空白失败）+ 历史按生成批次分组浏览（对症一次生成 4 张难对比）。后端零改动，jsonl 单行模型、hashRef 主键、locked 锁定语义全部不变。

## 2. 交付概览

| 项 | 状态 |
|---|---|
| PRD | ✅ `docs/资产增强-增量PRD-2026-08-18.md`（验收 A1-A4，P0 仅 R2+R3） |
| 实施设计 | ✅ `docs/资产增强-实施设计-2026-08-18.md`（+ class/sequence mermaid） |
| 代码实现 | ✅ commit `1e7922f`：9 文件 / +607 行 / 后端零改动；`tsc` + `vite build` 通过，无循环依赖警告 |
| QA 回归 | ✅ `smoke/qa-r2r3-asset-enhance.cjs`：77/77 断言通过，智能路由判定 **NoOne**（源码无 Bug） |
| 已知问题 | 0（1 条已知行为观察，非缺陷，见 §4） |

## 3. 改动文件清单（commit 1e7922f）

| 文件 | 改动 |
|---|---|
| `src/v1/types/flow.d.ts` | ImageAssetRecord +9 可选配方字段；HistoryEntry image 分支 + `batchId?`；AdoptMeta 注释更新 |
| `src/v1/asset-store.ts` | adopt() 写配方入记录（_applyRecipe 合并语义，在 adopted return 之前）；_normalize 配方容错；新增 metaFromNode / recipeFromRecord；getAdoptedAssets meta 合成 |
| `src/v1/canvas/card-view.ts` | 角标采纳改传 `assetStore.metaFromNode(node)`（原 undefined，复现空白根因） |
| `src/v1/ui/compare-panel.ts` | 格内采纳改传 `assetStore.metaFromNode(node)` |
| `src/v1/ui/asset-drawer.ts` | 卡片配方信息区 + 复制配方按钮 + 配方缺失占位；_toEntry 取数优先级 记录→meta→反查 |
| `src/v1/engine/run-engine.ts` | runBatch/runOutpaint 生成 batchId；runOneWorker/createResultCard/_writeBackToSelf 透传；text/反推/旧图不带 |
| `src/v1/ui/history-drawer.ts` | batchId 透传；view 状态（默认按批次）+ 切换控件；_buildDisplay 分组；_renderBatchCard 批次卡（含逐图能力） |
| `src/index.html` | 历史抽屉 #history-view 视图切换控件 |
| `src/v1/styles/app.css` | .history-view / .history-batch* / .asset-recipe* 样式 |

## 4. 验收对照（PRD A1-A4）

| 验收 | 结果 | 证据 |
|---|---|---|
| A1 跨项目复现 | ✅ | adopt 带 meta → 记录含 9 配方字段 → 清空 metaByKey（模拟重启/切项目）→ recipeFromRecord 合成 meta 非空 → 复现可构造 |
| A2 批次分组 | ✅ | 4 条流水 → 批次卡（4 图 + 4/4 + 摘要）；time 视图 4 条平铺 |
| A3 批次卡单图能力 | ✅ | 逐图 查看大图 / 拖入画布（application/history-image）/ 复现（trace 保真） |
| A4 回归 | ✅ | hashRef 主键 / locked 锁定 / jsonl 单行模型 均零改动；旧数据兼容容错 |

**已知行为（非本次引入，不建议返工）**：txt2img 第 1 张写回源节点自身 → in-session 历史抽屉少 1 行（jsonl 含 2 行带 batchId，重载后批次卡 2/2）。img2img（"一次生成 4 张"主场景）in-session 即为满计数。如需 in-session 也满计数需改设计（第 1 张也 addImage），记录为已知行为。

## 5. 测试方式

```bash
node node_modules/typescript/bin/tsc -p tsconfig.smoke.json --outDir .icv-qa
node smoke/qa-r2r3-asset-enhance.cjs
```

## 6. 下一步（路线）

② 图操分家主线改造（image/text/gen 三类节点）——最大风险是老 image-gen 节点迁移拆分，务必先在测试项目验证迁移、再碰真实数据（`docs/工作流改造方案-审查意见-2026-08-18.md` §五）。
