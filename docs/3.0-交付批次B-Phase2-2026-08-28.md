# Infinite Canvas 3.0 · 交付批次 B 报告（Phase 2 统一资源侧栏）

> 日期：2026-08-28 ｜ 团队：software-icv3（主理人编排，工程师寇豆码实施，QA 严过关独立验证）
> 依据：《3.0-Infinite Atelier 工作流重构实施规范.md》第 10 节交付批次 B

## TL;DR

完成 Phase 2 **统一资源侧栏**：左侧单一资源抽屉（资源 / 历史 / 提示词三页签）+ 插入规则统一到 `resource-insert.ts`。QA 独立验证全部通过（NoOne），零 `.icproj` 格式变化，扩图/继续创作链路无回退。

## 交付概览

- **交付状态**：✅ 可交付（QA 路由判定 NoOne）
- **测试通过率**：typecheck 0 错误 ｜ build 成功（56 modules）｜ 扩图 smoke 21/21 ｜ 继续创作 smoke 37/37 ｜ 资源插入 smoke 45/45
- **已知问题**：0 阻塞 / 0 已知问题

## 验收矩阵

| 编号 | 内容 | 结论 |
|---|---|---|
| Phase2-1 | 资源拖入空白画布 → 素材节点，未触发生成 | ✅ PASS |
| Phase2-2 | 历史放到画布 → 可预览、可继续创作 | ✅ PASS |
| Phase2-3 | 资源拖到生成节点 → 只加参考图，不建意外节点 | ✅ PASS |
| Phase2-4 | 历史存资源后资源页即时出现；移出资源不删历史 | ✅ PASS |
| R-01 | 历史图保存到资源，配方带入 | ✅ PASS |
| R-02 | 从资源移除只动索引，画布/历史/磁盘不受影响 | ✅ PASS |
| R-03 | 历史图放到画布，可继续创作 | ✅ PASS |
| R-04 / R-05 | 项目保存重开 / 旧项目加载 | 未覆盖（persistence 本轮未触碰，无回归风险） |

## 修改文件清单

**新增**
| 文件 | 职责 |
|---|---|
| `src/v1/ui/resource-insert.ts` | 统一插入协调：insertImageAsAsset / insertHistoryImageToCanvas / attachImageToSelectedGeneration / attachImageToNode / startCreateFromResource；三处旧创建逻辑收敛于此 |
| `src/v1/ui/prompt-library.ts` | 提示词库共享 store（cmd-panel 弹窗与提示词页签共用，消除两份读写） |
| `src/v1/ui/prompt-tab.ts` | 提示词页签：搜索 / 插入当前节点 prompt / 复制 / 收藏当前输入 |
| `src/v1/ui/clipboard.ts` | 复制小工具 |
| `smoke/qa-resource-insert.cjs` | 45 项断言资源插入回归 |

**修改**
| 文件 | 职责 |
|---|---|
| `src/index.html` | 双抽屉合并为单外壳 + 三页签 + 单胶囊入口 |
| `src/v1/ui/left-capsule.ts` | 单入口/单外壳开合 + 页签切换编排 + setTabOpenRequest；切换保留查询关键字、关闭保留页签状态 |
| `src/v1/ui/asset-drawer.ts` | 并入统一抽屉，新增「放到画布」，保留移除/查看/复制配方/拖拽 |
| `src/v1/ui/history-drawer.ts` | 卡片四动作（复制/放到画布/存到资源/继续创作） |
| `src/v1/canvas/interactions.ts` | 拖拽/文件导入统一走 resource-insert，删除本地重复创建 |
| `src/v1/ui/cmd-panel.ts` | 提示词库改共享 store + outpaint 摘要 `?.` null 守卫（修批次 A 低危）+ getCurrentPrompt/insertPromptToCurrent |
| `src/v1/main.ts` | 初始化编排 + Escape 关闭抽屉 |
| `src/v1/styles/app.css` | 页签/提示词页样式（沿用暖白 token） |

## 「既有失败」独立核实结论

工程师自述 qa-text-wiring / qa-node-split / qa-imageperf 有既有失败，QA 亲自复跑并核实：
- **qa-text-wiring**：135 过 6 败 → 指向 run-engine.ts（未动）/ cmd-panel 已禁用预览（faff873 早于本批次）
- **qa-node-split**：崩溃 `getUpstreamOutputs is not a function` → flow-state.ts / persistence.ts（均未动）
- **qa-imageperf**：26 过 3 败 → api.ts（未动）
- 旁证：另跑 5 个相关 smoke，失败全部指向本批次未触碰文件，为陈旧测试断言（期望已被更早提交有意改变）

**结论：既有失败与本批次无关，不阻塞交付。**

## 兼容性与纪律

- **`.icproj` 格式变化：无**。未新增持久化字段；persistence.ts / flow.d.ts / asset-store.ts / history-persist.ts 全部未改动。
- 7.1 严禁项扫描通过：UI 无直接改 flowState.nodes/edges；无绕 runEngine 自拼请求；无新增持久化字段。
- 双抽屉合并后无死链（旧 id 在 src 全库零命中）。
- 原工作区无关改动：历史文档删除记录、3 个未跟踪 3.0 文档、未提交小改动——均只报告未处理。

## 遗留项（非阻塞）

1. **清理残留**（批次 A 遗留，待用户授权）：`.icv-qa-current/`（已加 gitignore）、`smoke/qa-probe-edge.cjs`。
2. 未补 DOM headless 交互测试（卡片按钮点击/页签切换），核心语义已由状态/引擎级 smoke 覆盖，建议人工试用确认。

## 用户下一步建议

1. **启动应用试用**：左侧应只有**一个**资源入口，展开后是「资源 / 历史 / 提示词」三页签；试拖一张资源图到空白画布（建素材）、拖到图片生成节点（只加参考图）、历史卡「保存到资源」后资源页即时出现。
2. 确认三页签切换不丢搜索词、关闭再开后还在原页签。
3. 试用确认后我继续 **Phase 3（连线可读：参考/文字/结果三种语义视觉区分）**。
4. 清理残留文件（`.icv-qa-current/`、`smoke/qa-probe-edge.cjs`）授权我删除即可。
