# 4.0 QA 验收报告（Infinite Atelier 功能复刻 · 全量回归）

> 验收人：QA 严过关（Edward）
> 日期：2026-08-28（首版）；2026-08-28（P2-1/P2-3 修复复验更新）；2026-08-28（审批退回 P1/P2 复审复验更新）
> 依据：4.0 总控 §6、4.1/4.2/4.3/4.4 四卷验收条目、架构师《4.0-现状审计与任务分解》§4、《4.0-审批意见-2026-08-28》复审准入
> 方式：**只验证不修码**；构建/静态验证 + 读码核对 + 现有 smoke + 临时目录动态测试（不调用真实媒体 API）

---

## 0. 结论（TL;DR）

| 项 | 结果 |
|---|---|
| `npm run typecheck` | ✅ 0 错误（含 P2 修复后复跑） |
| `npm run build` | ✅ 成功（主应用 + 导演台双 bundle；1 条无害 warning） |
| Python 全量 `py_compile` | ✅ 20 个文件全部通过（含 P1/P2 修改文件复跑） |
| `smoke/director_smoke.py` | ✅ ALL PASS（8 项） |
| 4.3-D schema 动态测试 | ✅ 7/7 通过 |
| .icbundle 安全动态测试 | ✅ 6/6 通过 |
| P2-1 复验（check_recent_project_path） | ✅ smoke 5/5 + 读码通过 |
| P2-3 复验（delete_temp_file 白名单） | ✅ smoke 6/6 + 读码通过 |
| **P1 复验（director 保存接口校验）** | ✅ smoke 7/7 + 读码/边界探针通过 |
| **P2 复验（.icbundle 导入孤立资产清理）** | ✅ smoke 6/6 + 读码/边界探针通过 |
| 硬排除（高清放大） | ✅ 未发现同义入口 |
| **可否交付** | **✅ 可以交付**（P0=0，P1=0【审批退回项已修复并复验通过】，P2 剩 2 项不影响发布） |

> 更新说明（2026-08-28 复审）：工程师已修复审批退回的 P1（`director_save_project`/`director_save_project_as` 保存前校验）与 P2（`.icbundle` 导入取消/写入失败时清理本次新建资产），
> QA 按《4.0-审批意见-2026-08-28》复审准入独立复验通过（见 §1.4 与 §4 遗留清单），P0/P1 结论与本次实际验证一致。

> 更新说明（首版）：工程师已修复 P2-1（最近项目失效检测）与 P2-3（蒙版临时文件清理），
> QA 于 2026-08-28 独立复验通过（见 §1.3 与 §3 遗留清单），原 4 项 P2 减为 2 项。

---

## 1. 执行过的命令与结果

### 1.1 构建与静态验证

| 命令 | 结果 | 备注 |
|---|---|---|
| `npm run typecheck` | ✅ 0 错误 | `tsc --noEmit` 干净退出 |
| `npm run build` | ✅ 成功 | `vite build` 主应用 `index-CEdmrLoF.js 807.12 kB`；`vite build --config vite.director.config.ts` 导演台 `director-director.js 1734.48 kB` + `mp4.worker-LR1KNGnj.js 2.84 kB` |
| `python -m py_compile backend/api/*.py backend/director_api.py main.py` | ✅ `PY_COMPILE_ALL_OK` | 15 个文件：audio_api/bundle_api/backup_api/image_api/model_rules/provider_api/unified_api/video_api/project_api/settings_api/utils/errors/gemini_compat/clipboard_api/director_api/main.py（Python312 含 tkinter）；复审轮扩为 backend 全量 + main.py + 新增 2 smoke 共 20 文件复跑通过（见 §1.4） |
| `python smoke/director_smoke.py` | ✅ `DIRECTOR SMOKE: ALL PASS` | 8 项：保存重开一致性 / 最近工程 / 资源校验 / 拒绝非 icdirector / 视频落盘 / 回传无主窗口报错 / launch options / ping |

### 1.2 补充动态测试（临时目录 + 假媒体，零真实 API 费用）

**4.3-D 能力 schema（7/7 通过）**
- 缺字段 schema → `validate_capability_schema` 拒绝（`kinds 至少选择一种能力类型` 等）✅
- 合法 custom-declarative → 校验通过 ✅
- 注入 `header` / `eval` 关键词 → 拒绝（`adapter 含被禁止的关键词「header/eval」`）✅
- schema 含 `api_key` → 拒绝（`schema 含被禁止的敏感字段「api_key」`）✅
- `bundle_api._without_secrets` 剥除 `api_key/authorization` ✅
- 自定义音频模型（my-music, audio kind, custom-declarative）→ 校验通过 + `build_custom_adapter_preview` 正常 ✅

**.icbundle 安全契约（6/6 通过）**
- 导出成功，SHA-256 去重落 `assets/<sha>.png`，manifest 含 project/asset 哈希 ✅
- 包内搜索不到 `api_key` / `sk-...` 明文 ✅
- 导入为新建项目成功，`imageOrigin.path` 重写为本机路径，项目 JSON 无 Key ✅
- 恶意 ZIP（`../evil.txt` 穿越）→ 拒绝（`unsafe archive`）✅
- 恶意 ZIP（project 哈希不匹配）→ 拒绝（`bad project hash`）✅
- `_safe_name` 对 `../x` / `/abs` / `a/../b` 全部拒绝，仅接受正常相对路径 ✅

### 1.3 P2-1 / P2-3 修复复验（2026-08-28 第二轮）

**复验命令**
- `npm run typecheck` → ✅ 0 错误（P2 修复后复跑）
- `python -m py_compile backend/api/settings_api.py backend/api/image_api.py main.py` → ✅ RC=0
- `smoke/qa_p2_1_check_path.py` → ✅ `CHECK_RECENT_PATH_SMOKE: ALL PASS`（5/5）
- `smoke/qa_p2_3_delete_temp.py` → ✅ `DELETE_TEMP_FILE_SMOKE: ALL PASS`（6/6）

**P2-1 最近项目失效检测（复验通过 ✅）**
- `src/v1/ui/project-home.ts`：旧 `exists()`（只查 `\0`，L70）已删除；`renderRecents` 改为 async + `Promise.all` 预取 `Backend.checkRecentProjectPath` 生成 existsMap（L51-79）；失效项目 → `is-missing` 类 + `main.disabled` + 「文件不可用」文案；`clearMissing` 用真实 `existsPath` 检测过滤（L95-104）
- 后端不可用/查询异常 → catch 回退 `true`（L60-62/L87-89），**不误标红**，openPath 兜底不变
- `backend/api/settings_api.py:139-148` `check_recent_project_path`：只读 `os.path.exists`，非法/异常按不存在处理（`exists:False`），绝不删除或改写
- 桥接全链路：`main.py:662` → `src/utils/api.ts:97/298` → `src/v1/api.ts:517`
- 动态 smoke 5/5：存在=True 且只读（mtime 不变）/ 缺失=False / 空路径=False / None=False / 相对路径归一=True

**P2-3 蒙版临时文件延迟清理（复验通过 ✅）**
- `backend/api/image_api.py:362-391` `delete_temp_file`：路径白名单仅允许「图片保存目录 `image_save_path`」或「会话临时导入目录 `tempfile.gettempdir()/infinite_canvas_imports`」；前缀匹配用 `root + os.sep` 防兄弟目录绕过；文件不存在幂等成功；异常返回 error
- `src/v1/ui/image-editor/mask-editor.ts`：`createdMasks/succeededMasks/scheduledMasks` + `scheduledMaskTimers` 去重（L49-56/L140-148）；提交成功 → `succeededMasks.add` + **10 分钟**延迟清理（L478-479，任务完成前绝不删，trace 仅记路径、结果图独立落盘）；用户取消 → 1s（L487）；失败 → 60s（L491）；关闭/放弃 → 未成功文件 1s 清理（L121-124），成功文件不受影响
- `prepareImportedImage` 落盘目录 = `image_save_path` 或 `infinite_canvas_imports`（image_api.py:80-88/158-161），与白名单一致 → 清理可命中
- 桥接全链路：`main.py:509` → `src/utils/api.ts:69/213` → `src/v1/api.ts:308`
- 动态 smoke 6/6：白名单内可删 / 白名单外拒绝且保留 / **兄弟目录前缀攻击拒绝** / 导入目录可删 / 幂等（已不存在成功）/ 空路径 error

**复验结论：两处 P2 均已修复，无新引入问题。**

### 1.4 审批退回 P1/P2 复审复验（2026-08-28 第三轮）

**复验命令（环境：系统 Python 3.12.10 含 tkinter，`C:\Users\17998\AppData\Local\Programs\Python\Python312\python.exe`）**
- `npm run typecheck` → ✅ 0 错误
- `npm run build` → ✅ 双 bundle 成功（`index-DjJqyBHl.js 809.54 kB` + `director-director.js 1734.48 kB` + `mp4.worker-LR1KNGnj.js 2.84 kB`）；`EMPTY_IMPORT_META` 警告仍出自 `src/director/export/mp4.ts`（构建工具显示行号 44，源码 `import.meta.url` 位于 mp4.ts:88），属既有遗留，不作为退回原因
- `python -m py_compile`（backend 全量 + main.py + 新增 2 smoke，20 文件）→ ✅ `PY_COMPILE_ALL_OK`
- `python smoke/qa_p1_director_save_validation.py` → ✅ `DIRECTOR_SAVE_VALIDATION_SMOKE: ALL PASS`（7/7）
- `python smoke/qa_p2_bundle_import_cleanup.py` → ✅ `BUNDLE_IMPORT_CLEANUP_SMOKE: ALL PASS`（6/6）
- 回归：`smoke/director_smoke.py` 8/8 ✅、`smoke/qa_p2_1_check_path.py` 5/5 ✅、`smoke/qa_p2_3_delete_temp.py` 6/6 ✅、`smoke/test-trust-layer-backend.py` 24 项 ✅

**P1 director 保存接口校验（复验通过 ✅）**
- `backend/director_api.py:52-62` `_validate_director_path`：非空字符串 + `.icdirector` 扩展名（大小写不敏感）；`not-a-director.json`/无扩展名/空/非字符串均拒绝
- `:64-71` `_validate_director_data`：data 为 dict 且 `format=='icdirector'`；非对象/错误 format 均拒绝
- `:73-92` `director_save_project`：`path` 为空 → 仍返回 `need_save_as`（约定保持）；校验失败在 `atomic_write_json` 之前返回 error，不创建/覆盖文件，**无 .tmp 中间文件**
- `:94-120` `director_save_project_as`：非法数据**弹窗前**直接拒绝；对话框取消仍返回 `cancelled`；写盘前二次校验路径
- 绕过面核查：backend 仅此两处写 `.icdirector`（grep 全仓确认），`main.py` 将 `DirectorAPI` 直接作为 `js_api` 透传，无绕过包装；前端 `project-store.ts` `createBlank`/`migrate` 恒置 `format: DIRECTOR_FORMAT('icdirector')`，`serialize()` 全量往返 → 前端无需改动（工程师说法成立）
- 动态 smoke 7/7 + 额外边界探针：空/None path → need_save_as；大写扩展名可保存；合法保存无 .tmp 残留；save_as 非法数据弹窗前拒绝（无 GUI）

**P2 .icbundle 导入孤立资产清理（复验通过 ✅）**
- `backend/api/bundle_api.py:250` `created_assets` 回滚清单：仅 `not os.path.exists(dest)` 且 `_atomic_copy` 成功后才入清单，去重命中（已存在）绝对不进清单
- 取消（`:304-306` 路径选择返回 None）、insert 模式非法（`:291-293`）、任意异常（`:316-321`）均调用 `_remove_created_assets` 清理本次新建资产；`project_written` 标志保证项目写盘成功后不回滚（资产已被新项目引用）
- `:458-470` `_remove_created_assets`：仅删清单内文件，`os.path.isfile` 守卫，幂等，绝不删导入前已存在的去重资产；`:472-486` `_atomic_copy` 失败清理 .tmp 后抛回
- insert_canvas 成功路径保留资产合理：`assets` 随返回值交前端引用（撤销栈保证原子性）；new_project 写盘成功后不回滚合理
- 原始错误记录：`print(f'导入资源包失败: {exc}')`（`:320`）保留现场
- 动态 smoke 6/6 + 额外边界探针：`_atomic_copy` 失败无 .tmp 残留；`_remove_created_assets` 幂等；清理清单只含本次新建文件

**复验结论：审批退回的 P1/P2 均已修复，无遗漏、无绕过路径、未发现新引入回归；P0/P1 结论更新为与实际验证一致（P1 已修复并复验通过）。**

---

## 2. 逐切片验收矩阵

图例：✅ 通过 ｜ ⚠️ 警告（附原因）｜ ❌ 失败（附文件:行号+证据）

### 卷 4.1 图像编辑与画布生产力

| 验收点 | 结果 | 证据 |
|---|---|---|
| A1/A2/A3 裁剪/切图/小地图（复验） | ✅ | 架构师已审计实现；typecheck/build 通过；image-editor.ts / canvas-view.ts minimap 存在；本回归未发现退化 |
| **B1 蒙版局改**：accepted 后只轮询原任务不重投 | ✅ | `image-edit-engine.ts:124-154` `submit()` 仅一次 `Backend.generateImageEdit` 后 `pollTask(taskId)` 只查询；`recover()` 也只查原任务（L91-105） |
| B1：取消=停本地等待 | ✅ | `image-edit-engine.ts:82-88` `cancel()` 移出 active + `status:'idle'`，错误注明"远端任务不会被重复提交"；mask-editor `close()` 同语义 |
| B1：失败保留蒙版/提示词 | ✅ | `mask-editor.ts:448-454` 失败后 `running=false`，蒙版画布与 prompt 不清空，可修改重试；`start()` 失败返回 `ok:false` |
| B1：错误带 remoteTaskId | ✅ | `image-edit-engine.ts:175-186` 失败/不确定时拼接 `（远端任务：${remoteTaskId}）` |
| B1：蒙版编辑器交互（笔刷/橡皮/撤销/半透明/缩放/清空） | ✅ | `mask-editor.ts` 全交互齐备（L193-369）；提交前展示图+蒙版+提示词+模型 |
| B2 多角度：无图片参考能力不显示入口 | ✅ | `action-bar.ts:154-155` `act-hidden` 按 `getImageEditCapabilities(...).mask/imageReference` 显隐；`angle-editor.ts:53-54` 二次拒绝 |
| B2：预设/自定义/不保证几何一致提示/trace | ✅ | `angle-editor.ts` 7 预设 + 自定义 + 提示文案（L110/L141）；trace 写 `angle:{preset,instruction}`（image-edit-engine.ts:66） |
| B3 @素材：结构化保存/解析 | ✅ | `cmd-panel.ts:500-509` `mentions[]` 写入 params；`run-engine.ts:237-266` `_resolveMentionImages` 解析为 referenceImages；文本类 mention 附加 prompt（L268-280） |
| B3：丢失标红阻止提交 | ✅ | `cmd-panel.ts:512-518` `_isMentionMissing` + `:556-574` chip `missing` 类 + `:779-785` `_onSend` 阻止 |
| B3：同名按 id 绑定 | ✅ | `cmd-panel.ts:524-542` 按 `@label` 出现次数保留唯一 id 的 mention；`_insertMention` 用唯一 id（L750-769） |
| B3：@ 触发选择器/纯文本降级/项目恢复绑定 | ✅ | `cmd-panel.ts:234-246` 输入 `@` 触发；纯文本降级 `appendMentionText`；mentions 持久化于 params，恢复后仍绑定 |
| **C .icbundle 导出/导入** | ✅ | `bundle_api.py`：`_safe_name` 路径穿越校验（L40-48）、SHA-256 去重/校验（L51-56/345-359）、原子回滚（导出 temp+`os.replace` L144-172；导入 staging 校验后才写 L263-301）；`_without_secrets` 剥 Key（L59-64）；**6/6 动态测试通过**；UI `export-bundle.ts`/`import-bundle.ts` + 操作条/右键入口 |

### 卷 4.2 视频与音频画布

| 验收点 | 结果 | 证据 |
|---|---|---|
| A1 视频模型/设置/能力 schema | ✅ | `model-config.ts` `getVideoModelCapabilities`（supportsImageReference/StartEndFrame/Audio/seconds/resolutions/aspectRatios）；settings-panel 视频模型/Key |
| A2 节点与运行：状态机细分 submitting/accepted/processing | ✅ | `run-engine.ts:354-466` `queued→submitting→accepted→poll`；`_normalizeMediaState`（L573-582）；cmd-panel `_runHint`（L1074-1093）细分"提交中/已提交/处理中/排队中/查询恢复中" |
| A2：accepted 后只轮询原任务不重投 | ✅ | `video-poller.ts:14-52` 只 `getVideoTaskResult`；查询异常 `recovering` 重试同任务，20min→uncertain；`recoverMediaTasks` 只查原任务（run-engine.ts:612-700） |
| A2：三入口创建视频节点 | ✅ | interactions/action-bar/cmd-panel 均接入（action-bar.ts:39, 312-320） |
| A3 视频呈现：播放/下载/复制提示词/继续创作/加入资产/重新定位 | ✅ | `video-viewer.ts` 全动作齐备（含 `add-asset` L49、`relocate` L84-102）；画布卡 poster/时长/比例（card-view） |
| A：首尾帧门控 | ✅ | `video-gen.ts:29-30` 仅 `supportsStartEndFrame` 透传；`cmd-panel.ts:141-166` frame-strip 动态创建按能力显示；切换模型清除不兼容值并提示（L885-891） |
| A：每 Key 并发 1 | ✅ | `video_api.py:39-53` `_VIDEO_TASKS_PER_KEY=1` + `BoundedSemaphore` 按 (provider.id,key.id) 索引，不以明文 Key 为索引 |
| A：close-guard 媒体任务检测 | ✅ | `close-guard.ts:77-93` `runEngine.mediaTasksInProgress()` 检测进行中/待恢复任务（含跨会话 `params.videoTask/audioTask`），三选一弹窗含"关闭但保留远端任务记录" |
| **B1 音频后端 adapter** | ✅ | `audio_api.py` 三段协议 + `AUDIO_ADAPTERS`（fluxport_audio/openai_audio）+ mutagen→ffprobe→None 元数据 + 分块下载 + base64 兜底落盘；`requirements.txt` 已加 `mutagen` |
| B2 音频节点：未配置音频模型不出现可运行按钮 | ✅ | `audio-gen.ts:22` `canRun` 返回 `'未配置音频模型，请先在设置中配置'`；`cmd-panel.ts:1005-1012` 发送钮 `disabled` + title 明确提示 |
| B2：导入音频不 base64 进 .icproj | ✅ | `resource-insert.ts:122-159` `insertMediaAsAsset` 只存 `mediaUrl/mediaPath/duration/mimeType`（大文件路径引用）；`persistence.ts` `normalizeAudio` 归一 |
| B2：音频可播放/下载/加入资产 | ✅ | `audio-viewer.ts` + asset-drawer 音频卡/播放（asset-drawer.ts:179-198） |
| **C 媒体资产/历史/关联** | ✅ | 资产抽屉 `MediaFilter 'all'|'image'|'video'|'audio'` + 搜索 + 拖入（asset-drawer.ts:24/137-213）；历史 video/audio tab（history-drawer.ts:92/318-445）；`FlowEdge.kind:'audio-ref'`（flow.d.ts:250）+ link-view `'音轨'` 标签（link-view.ts:29/43/68） |
| C：视频连音频但模型不支持→运行前明确提示 | ✅ | `run-engine.ts:363-373` 检测 audio 上游 + `!caps.supportsAudio` → `status:'fail'` + 明确错误"可切换支持音频的视频模型，或断开音频连线"，不静默丢弃 |

### 卷 4.3 提示词、资产、项目与模型配置

| 验收点 | 结果 | 证据 |
|---|---|---|
| A 视觉提示词库（复验） | ✅ | `prompt-library.ts` PromptCard 增删改/收藏/搜索/复制/封面（`savePromptCover`）；旧 localStorage 收藏迁移（L142-143）；三个插入动作（替换/追加/文本节点，prompt-tab.ts）；插入不自动运行 |
| B .icbackup（复验 + 4.3-D 补位） | ✅ | `backup_api.py` 导出含 `capability_schemas.json`（L134/159）+ `_without_secrets` 剥 Key（L39-43）+ `_restore_capability_schemas` 逐条校验恢复（L242-257）；导入 staging 校验、原子写入；冲突策略（copy/merge/skip 由架构师审计确认） |
| C1 最近项目（复验） | ⚠️ | 见 §3 遗留 P2-1：`project-home.ts:70` `exists()` 失效检测逻辑有缺陷（UI 永不标红/清理失效），打开时由 `persistence.openPath → Backend.loadProject` 兜底报错，无数据风险 |
| C2 主题（复验） | ✅ | `bottom-bar.ts` WORKSPACE_THEMES 5 套（暖白/深色/海雾/靛紫/铜绿）；`data-theme` + CSS 变量；持久化 settings.workspace_theme + localStorage 回退；主页切换（project-home.ts:76） |
| **D 模型能力 schema** | ✅ | `model_rules.py` `validate_capability_schema`/`normalize_capability_schema`（缺字段不可保存，实测通过）；custom-declarative 白名单（URL path 相对路径/字段映射白名单/`_INJECTION_WORDS` 禁 header/eval/脚本/原型链，实测通过）；`provider_api.save_capability_schema` 校验失败拒绝落盘（L681-699）；`capability_schemas.json` 随设置备份不含 Key（实测 `_without_secrets` 通过）；前端 `model-config.ts` 用户 schema 优先 + 内置回退（getVideo/getAudio/getImageEdit 能力门控仍工作，typecheck 通过）；settings-panel schema 编辑 UI（L911-940） |

### 卷 4.4 MONOFORM 导演台

| 验收点 | 结果 | 证据 |
|---|---|---|
| D0 壳/工程文件：无 Three.js 资源可创建/保存/重开空工程 | ✅ | `project-store.ts` createBlank/migrate(0→1)/open/save/saveAs；`director_api.py` `director_save_project` 原子写 + 拒绝非 icdirector（smoke 第 4 项） |
| D1 场景/视口/层级/变换/GLTF 导入限大小 | ✅ | `src/director/engine/scene.ts` + `import/gltf.ts`；smoke 保存重开位置/锁定/可见性一致 |
| D2 镜头/参考图：两相机不同构图/导出 PNG 与监看一致 | ✅ | `camera.ts`/`reference.ts`/`monitor.ts`；`export/png.ts` |
| D3 光照/人物：保存恢复/无骨骼降级 | ✅ | `lighting.ts` 默认恢复；`character.ts` 无人物时 UI 明确提示（main.ts:704-716） |
| D4 时间轴/导出：插值/取消后可用 | ✅ | `timeline.ts`+`timeline-ui.ts`；`export/mp4.ts` 独立 worker + 进度/取消（main.ts:836-873，取消后"应用仍可继续使用"） |
| D5 与主画布互通：回传成素材/trace 溯源/不暴露内部路径 | ✅ | `director-bridge.ts` 回传 PNG/MP4 走现有导入管线；trace `director:{projectId,shotId,cameraId,time}`；smoke 回传无主窗口明确报错 |
| WebGL 不可用降级路径 | ✅ | `director/main.ts:94-98` `viewport.init()` 失败 → `shell.showWebglFallback()` + toast，仍可新建/打开/保存工程（try/catch 明确降级，不阻塞主程序） |
| 独立窗口/独立 .icdirector | ✅ | `main.py:599-629` `director_open` 独立 pywebview 窗口；`.icdirector` 与 `.icproj` 完全隔离 |

### 硬排除（4.0 §1.2）

| 检查 | 结果 | 证据 |
|---|---|---|
| 高清放大/超分/super resolve/upscale 同义入口 | ✅ 未发现 | 全仓 grep `超分|super.?resolve|upscale|高清放大` 于 `src/` 与 `backend/` → 0 命中；`放大` 仅出现在 `mask-editor.ts:13/409` 蒙版缩放注释（合理用途，非功能入口） |

---

## 3. 回归风险点核查

| 风险点 | 结果 | 证据 |
|---|---|---|
| `flow.d.ts` 公共类型一致（NodeStatus/MediaAssetRecord/FlowEdge.kind/mentions） | ✅ | `NodeStatus` 含 `'submitting'`（L13）；`MediaAssetRecord.kind:'image'\|'video'\|'audio'`（L437）；`FlowEdge.kind?:'audio-ref'`（L250）；`PromptMention`/`StyleTransferParams.mentions`（L62-70/L309）；`VideoGenParams.startFrame/endFrame`（L93-94） |
| `model-config.ts` 内置+用户 schema 合并后 getVideo/getAudioCapabilities 仍工作 | ✅ | `findUserSchema` 优先 + 内置 `VIDEO_CAPABILITIES`/`AUDIO_CAPABILITIES` 回退（L227-293）；typecheck 通过；用户 schema 空表时回退内置行为不变 |
| `main.py` audio_*/bundle_*/capability_*/director_* 前缀互不覆盖 | ✅ | 方法全部独立命名：generate_audio/get_audio_task_result、export_bundle/import_bundle、capability_load/save/delete/test_adapter、director_open；`self.audio/self.bundle/self.director/self.provider` 均在 `__init__` 注入（main.py:199-210），无重名覆盖 |
| `port-types.ts` video-gen inputs 含 Audio、audio-gen 行存在 | ✅ | `video-gen:{inputs:['Text','Image','ImageList','Audio'],outputs:['Video']}`；`audio-gen:{inputs:['Text','Image'],outputs:['Audio']}`（L19-20） |
| 4.2 与 4.4 互踩 | ✅ | `src/director/*` 为全新独立目录（untracked new）；v1 侧与 director 无共享文件；`main.py` 对 4.2/4.4 均为追加独立方法；`git diff --stat` 38 个改动文件均属各自切片范围，未见双方覆盖同一逻辑块 |

---

## 4. 遗留问题清单

| 级别 | 编号 | 问题 | 状态与建议 |
|---|---|---|---|
| P0 | — | 无 | 不阻塞发布 |
| P1 | 1 | ~~`director_save_project`/`director_save_project_as` 保存前未校验目标扩展名与工程格式~~ | **✅ 已修复（2026-08-28 复审复验通过）**：`director_api.py` 新增 `_validate_director_path`/`_validate_director_data`，校验失败拒绝且不创建/覆盖文件（含无 .tmp）；`need_save_as`/`cancelled` 约定保持；新增 smoke 7/7 |
| P2 | 1 | ~~4.3-C 最近项目"失效检测/清理失效"UI 失效~~ | **✅ 已修复（2026-08-28 复验通过）**：`project-home.ts` 改用 `Backend.checkRecentProjectPath` 真实路径检测（Promise.all 预取，异常回退不误标红）；后端 `settings_api.check_recent_project_path` 只读 os.path.exists，异常按不存在处理；smoke 5/5 |
| P2 | 2 | **build 1 条 warning**：`src/director/export/mp4.ts` IIFE 下 `import.meta` 被替换为空对象（`EMPTY_IMPORT_META`） | 仍遗留。构建工具显示行号 44，源码 `import.meta.url` 位于 mp4.ts:88；worker 资产经 `__VITE_WORKER_ASSET__` 常量已正常 emit（`mp4.worker-*.js` 存在），功能无影响；可后续加 `transform.define` 消除 |
| P2 | 3 | ~~蒙版临时文件无延迟清理~~ | **✅ 已修复（2026-08-28 复验通过）**：`image_api.delete_temp_file` 路径白名单（仅 image_save_path / infinite_canvas_imports，前缀攻击防护）；`mask-editor.ts` 成功→10min、取消/关闭→1s、失败→60s，`scheduledMasks` 去重，成功任务完成前不删；smoke 6/6 |
| P2 | 4 | **音频真实供应商未配置**（架构师 R2 已知限制）：`AUDIO_CAPABILITIES` 表为空，所有音频模型 `available:false` | 仍遗留（非缺陷）。符合 4.0 §3.4 降级（未配置音频模型不出现可运行按钮）；用户可通过 4.3-D schema 配置自定义音频模型；属产品能力等待供应商确认 |

---

## 5. 结论

**4.0 全量回归验收通过，可以交付（含审批退回 P1/P2 复审复验通过）。**

- 构建与静态验证：typecheck 0 错误、build 双 bundle 成功、Python 20 文件编译通过、导演台 smoke 8/8 PASS。
- 四卷验收：4.1（B1/B2/B3/C）、4.2（A/B/C）、4.3（A/B/C/D，C 含 1 项 P2 警告）、4.4（D0-D5 + WebGL 降级）共 **14 个验收组全部达成**；补充动态测试（schema 7 项、bundle 安全 6 项）全部通过。
- 硬排除（高清放大）无同义入口；共享契约（flow.d.ts/model-config/main.py/port-types）一致；4.2 与 4.4 无互踩。
- 审批退回项：**P1（director 保存接口校验）与 P2（.icbundle 导入孤立资产清理）均已由工程师修复，QA 按复审准入独立复验通过**（新增 smoke：P1 7/7、P2 6/6；回归 smoke 全过；门禁全过），P0/P1 结论与本次实际验证一致（P1 已修复并复验通过）。
- P2 现剩 2 项（P2-2 build warning、P2-4 音频供应商未配置——后者为已知限制非缺陷），均不影响本次发布，建议排入后续迭代。
